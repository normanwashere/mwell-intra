import { readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
const root = path.resolve('outputs/sep11-app-layout');
const run = process.env.AUDIT_RUN ?? 'final';
if (!/^[a-z0-9-]+$/.test(run)) throw new Error('Invalid evidence directory');
const data = JSON.parse(await readFile(path.join(root, run, 'results.json'), 'utf8'));
const live = data.origin === 'https://mwell-intra-uat.vercel.app';
const escape = text => String(text).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]);
const roles = [...new Set(data.results.map(row => row.role))];
const passed = data.results.filter(row => row.passed).length;
const label = role => role.replaceAll('_',' ');
let html = `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Intra app-wide screen update</title><style>
*{box-sizing:border-box}body{margin:0;color:#182436;background:#f5f7fa;font:15px/1.5 Arial,sans-serif;letter-spacing:0}header{background:white;border-bottom:1px solid #cfd9e4;padding:24px max(24px,calc((100% - 1360px)/2))}h1{font-size:28px;margin:6px 0 10px}h2{font-size:19px;margin:0 0 12px}p{margin:8px 0}a{color:#075ebc;text-underline-offset:3px}.status{color:#88540e;font-size:12px;font-weight:bold}main{max-width:1408px;padding:24px;margin:auto}.summary{display:grid;grid-template-columns:1fr 1fr;gap:24px;padding-bottom:24px;border-bottom:1px solid #cfd9e4}.summary ul{margin:0;padding-left:20px}.filters{display:flex;align-items:end;gap:16px;flex-wrap:wrap;padding:24px 0}label{font-size:13px;font-weight:bold;display:grid;gap:6px}input,select{font:inherit;padding:10px;border:1px solid #aab8c8;border-radius:4px;min-width:180px;background:white}input{width:280px;max-width:100%}.gallery{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:20px}article{border:1px solid #cfd9e4;border-radius:6px;background:white;overflow:hidden;min-width:0}article>a{height:260px;display:block;background:#eaf0f6}img{width:100%;height:100%;object-fit:contain}.caption{padding:14px}h3{font-size:15px;margin:0 0 8px;text-transform:capitalize}.route{font-size:12px;overflow-wrap:anywhere;color:#506277}.pass{color:#006f52}.fail{color:#a41632}details{padding:20px 0;border-top:1px solid #cfd9e4;margin-top:24px}summary{cursor:pointer;font-weight:bold}a:focus-visible,input:focus-visible,select:focus-visible,summary:focus-visible{outline:3px solid #117d9e;outline-offset:3px}[hidden]{display:none!important}@media(max-width:900px){.gallery{grid-template-columns:1fr 1fr}}@media(max-width:600px){.gallery,.summary{grid-template-columns:1fr}}
</style><header><div class="status">LOCAL CANDIDATE · LIVE UAT NOT UPDATED</div><h1>Intra app-wide screen update</h1><p>Actual working screens across the application, not a Knowledge Base redesign.</p><p><a href="http://localhost:3022">Open local app preview</a> · ${passed}/${data.results.length} captured screen checks passed · ${roles.length} test personas · Desktop 1440px and mobile 390px</p></header><main>
<section class="summary"><div><h2>What changed</h2><ul><li>One shared, compact header and section hierarchy across modules.</li><li>Smaller Home/Admin launchers and Warehouse shift shortcuts.</li><li>Consistent counter spacing, table density and filter placement.</li><li>Unframed operational sections in Procurement, Finance, Warehouse and Admin.</li><li>Aligned Event and Insights rows for quicker desktop comparison.</li><li>Existing actions and business flow retained.</li></ul></div><div><h2>What these checks mean</h2><p>These captures check rendered headings, viewport fit and JavaScript errors. They cover 46 primary role/route pairs across all 11 test personas, not every record, modal or transaction state.</p><p>Local code reads UAT data. Normal sign-in and learning-assignment synchronization are allowed; already-earned shared orientation records may synchronize. Operational writes and assessment submissions remain blocked. No warehouse release, approval, payment or migration was performed by this screen run.</p><p>Initial development captures are retained separately; the final run waits for settled headings and uses normal page initialization.</p></div></section>
<div class="filters"><label>Role<select id="role"><option value="">All roles</option>${roles.map(role=>`<option value="${role}">${escape(label(role))}</option>`).join('')}</select></label><label>Screen size<select id="width"><option value="">Desktop and mobile</option><option value="1440">Desktop</option><option value="390">Mobile</option></select></label><label>Screen or module<input id="search" type="search" placeholder="Search routes"></label><span id="count" aria-live="polite">${data.results.length} screens</span></div>
<div class="gallery">${data.results.map(row=>`<article data-role="${row.role}" data-width="${row.width}" data-route="${escape(row.route ?? '')}">${row.screenshot?`<a href="final/${row.screenshot}" aria-label="Open ${escape(label(row.role))} ${escape(row.route)} ${row.width}px screenshot"><img loading="lazy" src="final/${row.screenshot}" alt="${escape(row.route)} at ${row.width}px"></a>`:''}<div class="caption"><h3>${escape(label(row.role))}</h3><p class="route">${escape(row.route ?? 'Login')} · ${row.width ?? ''}px</p><strong class="${row.passed?'pass':'fail'}">${row.passed?'Passed':'Needs review'}</strong>${row.error?`<p>${escape(row.error)}</p>`:''}</div></article>`).join('')}</div>
<details><summary>Evidence and remaining limits</summary><p><a href="final/results.json">Final DOM results</a> · <a href="screens/results.json">Initial captures</a> · <a href="unit-tests.log">Application tests</a> · <a href="warehouse-tests.log">Warehouse tests</a></p><p>A shared layout change is not proof that every workflow, control or screen state has been certified. Existing exact-step screenshot certification, the pending readiness migration and a real-user usability pilot remain separate. This page is not a deployment record.</p></details></main>
<script>const controls=['role','width','search'].map(id=>document.getElementById(id));for(const c of controls)c.addEventListener('input',()=>{let count=0;document.querySelectorAll('article').forEach(a=>{a.hidden=Boolean(controls[0].value&&a.dataset.role!==controls[0].value||controls[1].value&&a.dataset.width!==controls[1].value||controls[2].value&&!a.dataset.route.toLowerCase().includes(controls[2].value.toLowerCase()));if(!a.hidden)count++;});document.getElementById('count').textContent=count+' screens';});</script></html>`;
// These captures predate the final module stylesheet and harness corrections.
if (!live) html = html.replace('http://localhost:3022', 'http://localhost:3021')
  .replace('Open local app preview', 'Open existing local development app')
  .replace('captured screen checks passed', 'pre-final geometry checks passed; screenshot acceptance still pending')
  .replace('Final DOM results', 'Pre-final DOM results')
  .replaceAll("class=\"pass\">Passed", "class=\"status\">Geometry passed; retake pending")
  .replace('Initial development captures are retained separately; the final run waits for settled headings and uses normal page initialization.', 'These screenshots were captured before the final stylesheet fix. Visual review found missing Events desktop styles and test-harness-blocked read requests. Both were corrected, the production build passed, and the harness now rejects blocked-request captures. The environment blocked restarting the production preview, so post-build retakes remain pending. These images are not final clean-screen certification.');
if (live) html = html.replace('LOCAL CANDIDATE · LIVE UAT NOT UPDATED', `LIVE UAT · ${escape(data.health.commit.slice(0,7))}`)
  .replace('http://localhost:3022', data.origin + '/work')
  .replace('Open local app preview', 'Open My Work on UAT')
  .replace('46 primary role/route pairs', `${new Set(data.results.map(row => row.role + ':' + row.route)).size} primary role/route pairs`)
  .replace('Local code reads UAT data.', 'These checks ran against the deployed UAT release, including My Work view changes, search entry and browser Back.')
  .replace('Final DOM results', 'Live DOM and interaction results')
  .replaceAll('final/', `../sep11-app-layout/${run}/`)
  .replace('href="unit-tests.log"', 'href="retests.log"')
  .replace('href="warehouse-tests.log"', 'href="shell-final-tests.log"')
  .replace('Warehouse tests</a>', 'Final shell tests</a>')
  .replace('href="screens/results.json"', 'href="../sep11-app-layout/screens/results.json"')
  .replace('the pending readiness migration and a real-user usability pilot remain separate. This page is not a deployment record.', 'and a real-user usability pilot remain separate. The ownerless-request readiness correction was applied to UAT and verified separately. Backend health confirmed the UAT project and release shown above. This is not all-transaction certification.');
await writeFile(live ? path.resolve('outputs/sep11-mywork-release/index.html') : path.join(root,'index.html'),html);
console.log(JSON.stringify({passed,total:data.results.length,roles:roles.length}));
