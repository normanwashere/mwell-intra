// Shim → @intra/ui: the Icon set moved to the shared design system (Step 1a).
// Re-exported so ported files that import `@/components/Icon` keep resolving.
export { Icon, type IconName } from '@intra/ui';
