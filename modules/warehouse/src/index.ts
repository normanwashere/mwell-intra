// @intra/warehouse — Step 2: full-parity port of mwell-intra-warehouse.
// Pure domain (from src/domain/*) + Next.js routes mounted under /warehouse/*.
// Reuses @intra/data-kit (offline) + @intra/ui; re-establish PWA via Serwist.
export {};
