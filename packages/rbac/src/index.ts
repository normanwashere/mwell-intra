// @intra/rbac — Step 1b will implement scoped Capability/role matrices + can().
// Contract (spec §4.2): capabilities are per-module; users can hold different
// roles per module; can(module, role, cap) mirrors core.role_capabilities.
export {};
