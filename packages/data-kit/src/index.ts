// @intra/data-kit — Step 1d: port warehouse data layer (repository port +
// DTOs, createRepository, in-memory adapter, IndexedDB outbox, runAction).
// Runtime-agnostic; carries the offline guarantees (spec §12 step 2).
export {};
