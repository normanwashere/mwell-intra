// @intra/auth — Step 1c: SessionProvider, JWT parse (app_metadata.roles),
// useSession(), <Guard module cap>. Live contract is JWT-only; demo/tests use
// the in-memory adapter (spec §5).
export {};
