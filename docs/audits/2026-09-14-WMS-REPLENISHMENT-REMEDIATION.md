# Warehouse Replenishment Follow-Up - September 14

## Status

### Latest Live Checks

**Database-first rollout started:** Only UAT migration `20260913214911_govern_replenishment_request_completion` was applied. Readback at `2026-09-13T21:49:28Z` confirms the unchanged canonical creator (`41fd0c1b11740e29164c2bec3972965e`) and public wrapper (`bcb0f480d1e7206457f42ec8bb76e4f7`), new inner implementation `93e04dc1eb0d1b4546aec258df31584c`, and private helper `a3c5dfd9b8f29bb6cc1d4690cbf83b59`. Existing execute grants remain; the private helper denies direct execution to anonymous, authenticated and service roles. The CLI-generated local file was renamed to the observed remote version with identical SQL SHA256 `379833866b51634ee452966e50f0405873bd999c75b6ff3e5bc27bacc06b67e8`; only test version references changed. All 121 completion-SQL/runner tests passed afterward. No existing draft was repaired and no new business fixture was seeded. UI deployment and fresh live completion remain pending at this entry. The earlier candidate-only statements below are historical. Raw before/apply/readback receipts are retained in `completion-*-apply-20260913T*.json`.

**Latest CI outcome (supersedes running-job notes below):** Run `34780214422` is terminal. Preparation and all six route jobs passed. Desktop and mobile governed transactions failed; both independent residue-cleanup jobs passed; the evidence bundle failed at `2026-09-13T21:29:54Z`. Transaction failure details are under separate review. Cleanup job success is not a claim that every retained WMS fixture or upload was removed.

Independent `ci188-transactions-2026-09-13T21-32-41-296Z/READOUT.md` and parent JSON checks confirm 44/45 desktop workflows and 45/45 mobile workflows report success. Only desktop `legal vendor invite` is false, recording `delivery_failed` / `email rate limit exceeded`. Mobile's invite is deliberately interaction-only and sends nothing. Both scenario arrays have only `vendor-accreditation` incomplete; email repair alone would not prove its missing application-readback and Legal handoff. The four broad warehouse scenarios pass their existing registry checks, which do not include replenishment completion or complete ecommerce/replacement journeys. Task 3's 17 passing results per viewport overlap the workflow count and are not extra independent journeys. Full CI remains red; this is not full WMS signoff.

Both in-run cleanup summaries contain 18 zero-remaining results. Each independent cleanup report has 73 zero-remaining results and removed nothing further, including three empty Storage scopes. This is marker/parent-scoped discovery, not an exhaustive table or orphan-object scan, archive/download proof or removal of retained WMS batches. All four downloaded ZIP hashes match GitHub metadata, extracted JSON CRCs were checked, and original artifacts remain unchanged. The readout records exact hashes and cleanup limitations.

After the latest home-link and documentation changes, the direct application build again passed TypeScript and all 21 static pages (223 precache entries, 17,021.49 KiB). Parent reran the frozen replenishment suite: 96/96 passed. The consolidated handbook was regenerated from 43 sources; its freshness check and 98 documentation/catalog/release tests passed. These are local results, not a new deployment or live completion.

The actual six route reports contain 186 WMS observations across 13 unique pages, not every WMS page for every role. There are no gated route/visual failures, but the desktop/tablet brand home link is only 28px high and falls outside the mobile-only target-size gate. Seven selected WMS screenshots were independently inspected; the 782 referenced WMS frames were not all visually reviewed. Route-only reports also misleadingly label empty transaction coverage and cleanup as complete. The bundle independently requires transaction and cleanup evidence, so no full-certification bypass is demonstrated. A separate producer/consumer correction is in progress to report these as not run or not applicable without relaxing required transaction checks. Original artifacts remain unchanged. Evidence: `ci48ca-34780214422-1789333795058/READOUT.md` and `route-analysis.json`.

**Local home-link improvement:** The Warehouse sidebar home link now has a 44px minimum height, with reduced surrounding padding keeping the row and following navigation in their original positions. Its destination and behavior are unchanged. All 21 component tests and ten actual-CSS browser cases passed across light/dark and desktop/tablet/mobile visibility. Six local screenshots were independently reviewed. The fixture uses a fallback font and representative navigation; this is not live full-sidebar certification. The combined return-link/home-link browser suite passed 16 tests and is wired into the next CI preparation job. These changes are not deployed.

**Fresh replenishment runner approved:** Independent review passed all 96 offline tests on runner `98f7b52baa2f26c5292417293c0456cab246186a7ec12f706d9815fa07d15921` and test `53cb74f5cb4a3aabd7831b4fa96c9110fc7fefc916c604c6b54e1c26a29667df`. The guard now matches the wizard's actual default response count and reserves a handoff before any awaited preflight, preventing overlapping callbacks from dispatching twice. The source-derived payload and actual callback regressions are not a live UI journey or a two-session database race. Four deterministic PDFs passed independent parse/render checks. Database-first rollout, exact deployed-build binding and the new seven-stage parent readback publisher still require verification before live execution. No new fixture, Storage upload, migration or deployment was performed by these offline tests.

**Reporting correction reviewed, not released:** The five-file collector/verifier change now labels route-only transaction coverage `not-run` and cleanup `not-applicable`, both without completion credit. Explicit transaction/all runs fail if writes are disabled or required viewport evidence is absent. The consumer rejects empty or contradictory scenario evidence and any embedded cleanup row with nonzero, unknown or failed residue checks, even when Product and standalone cleanup look valid. Parent and independent reviewer passed 153 tests with one existing live-opt-in skip; syntax and scoped diff checks passed. Applying the new producer read-only to the original CI188 reports retains only the vendor coverage failures and their 18-row completed cleanup, with original byte hashes unchanged. No existing artifact, gate, business flow or SMTP behavior was changed by this local correction.

**Pre-rollout checks:** The current replenishment application candidate completed a direct Next.js production build, including TypeScript and 21 static pages, with exit zero (223 precache entries, 17,021.49 KiB). This is a local dirty-worktree candidate build, not a new UAT deployment. Fresh database metadata at `2026-09-13T20:57:25Z` matches the previously reviewed creator, public wrapper and inner implementation hashes/ACLs; the private completion helper is not installed. The security advisor snapshot reports only 13 informational RLS-without-policy findings and no warning/error entries. Both raw tool responses are retained under `pre-completion-*-48ca3a7-2057*.json`; no DDL, grant or business mutation was issued. CI `34780214422` now has successful preparation and all six route/visual audit jobs. Desktop governed transactions are running and mobile transactions are queued. Actual report coverage is being reviewed separately; a green route job is not transaction or full WMS certification.

**Previous live link matrix remains preserved:** The fourth and stopped attempt `return-targets-48ca3a7-1789331653940-c190a5ff` reports 11/12 cells passed, `complete:false`, 12 ordinary password logins, 54 own-learning bootstrap calls, no blocked requests and no allowed business writes. It checks Operations Associate Returns and Finance Controller Fulfillment at 1440, 1280, 768, 390, 360 and 320. Its failed Operations 320 case measures a 64px-high customer-case link at `y=-7` after Back navigation: upper-corner hit points are outside the viewport, while the center and bottom points reach the link. No covering element was identified. The capture records a late position change after initial centering. This motivated the targeted follow-up below; the original remains failed, not proof of a new application obstruction. No fifth full-matrix retry was performed.

**Targeted 320px follow-up passed:** `return-target320-48ca3a7-1789333487488-d03eecd7` completed one fixed Operations Associate cell on exact `48ca3a7`/UAT project, using the authenticated checkpoint UUID `f662a626-b57d-4990-961c-a086472efddb`. All four original-order/customer-case links passed exact size, visibility, hit testing, fresh post-keyboard-focus geometry, activation, destination and Back checks. Their measured sizes were 230x84, 230x64, 230x84 and 230x64. Parent and independent reviewer inspected all eight PNGs and verified byte lengths, hashes, capture timestamps, helper/read-guard provenance and cell sidecar. Both order-detail captures waited for settled full-viewport sheet geometry; scrolling and whole-dialog usability remain outside this test. The report records `complete:true`, `fullMatrixComplete:false`, one auth request, four own-learning bootstrap calls (all 200), 119 reads including 14 signing reads, zero blocked/business writes and no diagnostics. This is not a zero-write claim.

The helper's once-only recenter correction passed 31 offline checks with independent review, including persistent clipping, real overlays, continuing movement and 43px failures. All four targets in the live follow-up reported `recentered:false`: this closes the targeted coverage gap but does not prove the earlier scroll writer or exercise the corrective branch live. Three targets sit at y=0, clipping only the exterior focus-outline edge; full target/text and five post-focus hit points remain visible/reachable. Report SHA256 is `45bc2075b14504712a460f316e19b875dd33e65cade88e2963dcb55560f0082f`. Its helper and frozen guard match the reviewed hashes. The earlier prepared `return-target320-48ca3a7-1789333405079-9b0c1458` stopped before browser launch because command stdin was closed; its exact waiting process was terminated and the terminal exit confirmed, with a separate interruption record retained. It is not an app failure. All owned live/test browser sessions are closed.

All earlier link attempts remain preserved: first `0ba6bfbe` blocked source-supported Storage signing reads and is failed; second `652bf89a` measured two desktop rectangles as `43.999969px` during route transition despite computed minimum 44px; third `063debb5` settled those rectangles but failed Operations 320 initial route readiness without a verified identity. The fourth uses fresh ordinary sign-in per cell, loaded fonts, bounded stable geometry and trial-click actionability while keeping the 44px comparison unchanged. These test corrections neither alter app code nor waive a genuinely undersized or obscured control. Finance destination navigation into a separately unauthorized Returns route was not demanded or granted.

Independent final review verified the frozen report/helper hashes and all 12 cell records, and inspected 16 of 39 screenshots: one target per role and width, the failed 320 target, and three 320 destinations. All 36 measured targets meet 44px; 35 completed hit/focus checks, and Operations completed 23/24 activation-destination-Back checks. Finance's 12 links were focused and measured, not destination-tested. The 320 failure happened before activation of the last target. The helper centered before actionability/settling and did not recenter after late movement; no specific application scroll writer is established. The report's interception label overstates this outside-viewport result. Two opening-transition order-detail captures do not certify settled dialogs. Current image fingerprints were computed, but no original per-image hashes were recorded, so historical image integrity is not established. All browser sessions are closed; no further full-matrix run was made.

**Documentation preparation:** The KB's process article incorrectly assigned acceptance to Operations. The completion candidate now assigns it to authorized Procurement, documents both required completion permissions and the existing wizard, and separates draft creation from route confirmation, submission and approval. The four handbook/specification sources mark the completion instructions as prepared but not deployed. Two new/updated guidance assertions failed before the correction; all 85 focused KB tests passed afterward. No screenshot certification or training publication was added by editing these instructions.

Independent documentation review also corrected a P2 recovery-instruction mismatch: **Back to recommendation** is offered on initial-read failure, while a stopped save instructs reopening the planning page and checking the existing linkage and retained evidence. The final focused 85 tests pass, and independent review reran all 16 recommendation-guidance tests. The broader KB run passed 491 tests with one skip before that final wording correction. All 56 selected handbook/catalog/release tests passed, and consolidated HTML regeneration/freshness checks passed. The shared process-article helper still assigns broad reader-role metadata to every step; this latent metadata debt is not rendered as action ownership or used for authorization. Copy correction is not a claim that all owner metadata has been repaired.

**Replenishment code review approved, release pending:** The first ten-file completion candidate was held for two independently reproduced P2 blockers despite 37 SQL, 353 Procurement and 13 Warehouse tests passing. Its read-error message wrongly claimed no request existed, and an identity/permission change or unmount during the first awaited read could occur before upload without a fresh authority check. The corrected twelve-file candidate now describes an unknown/changed outcome and offers **Back to recommendation**, without an unverified draft link or automatic replay. It checks authority immediately after that read and before every upload. Independent review reran all 366 Procurement tests successfully and closed both findings. Tests cover initial-read and mid-batch actor, permission, loading and unmount changes; generic upload cleanup is unchanged. An upload already dispatched may still finish and remain for reconciliation; this is not cancellation. SQL and Warehouse candidate hashes are unchanged. No migration, deployment, role grant or repair of the two existing drafts has occurred. Database-first rollout, Storage-byte verification, true two-session concurrency and the fresh live completion journey remain open.

**Access recovery released and checked:** Application `48ca3a751e2983699cc87eb6e6ebca572e65d55a`, UAT deployment `dpl_GJjYpWHfEG3H48cDigt6sPGn41ih`, is READY. Fresh canonical health confirms that exact commit, `appEnv=uat`, project `kkoitlvydytdhlpxhuah` and reachable Supabase. This release contains the independently reviewed access recovery, four return-reference target changes, audit-fragment correction and matching documentation. It does not contain the separate replenishment handoff candidate or any database migration.

The live diagnostic `access-candidate-48ca3a7-1789330728759/results.json` passed desktop 1280 and mobile 390. Successful focus refresh retained the unsaved rationale; one injected capability-read 503 per view displayed **Could not verify warehouse access**, and **Retry access** received a successful capability response and restored the exact product page/action. The parent and independent reviewer inspected all four error/recovery screenshots and verified their hashes and helper binding. Business writes were blocked; 18 own-user learning-bootstrap calls were explicitly recorded. Initial readiness took 4,819ms / 3,704ms including login and navigation, not a pure performance benchmark. Successful raw health/auth/capability responses and page-error counts were not archived, so this is source-bound browser assertion and image evidence, not network replay or full certification.

The earlier diagnostic `access-candidate-48ca3a7-1789330642655` remains failed: its initial five-second heading assertion expired before failure injection (`injected=0`), while its later diagnostic screenshot showed the page ready. The follow-up uses a bounded 30-second initial readiness wait; it does not relax the application's eight-second verification deadline or the expected recovery outcome.

Full CI `34775851767` is terminal/failed, with both independent cleanup jobs successful. Fresh run `34780214422` targets `48ca3a7` and is in progress. The separate 12-cell live return-link review is running; local layout tests are not substituted for it. Recovery screenshots also exposed an existing unstyled **Recommend replenishment** entry (`btn-secondary` has no shared CSS definition). A one-class correction to the established outlined-button style passed a new regression after reproducing failure and all 33 recommendation tests; it is a local, unreleased candidate with no workflow change.

**Read-only draft supplement completed:** `draft-read-only-1789329363806/results.json` verifies the retained desktop and mobile drafts through the actual handed-off recommendation link, using the ordinary Procurement Lead account on exact build `4b2a8e1`. Desktop table rows and mobile list-item cards both show the bound product and quantity of 2 units. Four new screenshots capture the line items and the classification blocker; the parent and independent reviewer inspected all four and checked their hashes and run/build/request/actor bindings. Ordinary-user request reads exactly match the previous independent snapshots. No business mutation was sent; 12 own-user learning-bootstrap calls were recorded. This is a completed read-only supplement, not a repaired draft or full journey: `fullJourneyComplete:false`, with classification blocked in both views. The original transaction report remains `complete:false`. An earlier supplement attempt stopped at a test-only native-fetch `ok()` error; that failure is retained separately.

**CI visual causes separated:** Run `34775851767` passed preparation but all six route jobs and both transaction jobs failed. Desktop return-validation evidence identifies a wrapped-link overlap finding, not a failed return RPC assertion. At desktop widths, the union rectangle's center falls between real text fragments; the audit mistakes the focusable containing `main` for an obstruction. A narrowly scoped collector correction samples each nonempty client rectangle and still fails when any sampled fragment is blocked. Independent review approved the exact two-file candidate after 23 Chromium tests and before/after probes at 1440 and 390. Parent combined verification passed 139 tests with one live-only skip. No viewport/role exemptions or target-size thresholds changed. These are local candidate results, not a passed rerun.

The mobile route reports separately identify real 40px-high return-case links, below the existing 44px requirement, in Returns (Operations Associate) and Fulfillment (Finance Controller). Their layout correction was subsequently released with destinations and handlers preserved, as recorded above. The separately downloaded mobile transaction artifact (`10324506473`, SHA256 `ddcbb69a2446bfecd08f48b265c2874cf9ff1bf9e29708e80da4044f81e88e92`) confirms that the return-validation workflow fails the same two 40px customer-case targets, not a return-validation assertion. It records no overflow, overlapping controls, dead links, console errors or network errors for that workflow. The failed report and its withheld scenario credit remain unchanged. Both cleanup jobs subsequently passed. SMTP/vendor scenarios remain outside dedicated WMS signoff, and broad CI is not certified.

**New P1 handoff defect, still open:** The third attempt (`19-37-05-115Z-19abafb8`) completed all six positive recommendation/acceptance/handoff commands and all 14 negative checks. Desktop opened the exact linked draft and verified its line. Mobile reached the exact draft but failed a desktop-only table-row assertion; the shared DataTable renders mobile cards. The report remains `complete:false`; no transaction replay is permitted against these decided fixtures.

Visual review of all seven saved screenshots then found a substantive problem not covered by that initial assertion: the newly created Procurement draft says **This legacy request is missing its requirement classification**. The current route decision cannot proceed. Fix the handoff's current draft contract and the authorized completion path; do not guess a legal classification, weaken required fields, or mark the journey passed because a draft row exists. Dirac is reviewing that mapping before any schema or business-data change.

Independent post-terminal reads for both views exactly match the recorded handed-off snapshots: each retains one synthetic product, one handed-off recommendation, one draft request and three activity rows; all 17 downstream counters are zero. Seven screenshots were visually inspected by the parent, not yet independently certified. The read receipts, rejected-action evidence and failed final assertion are retained in the attempt. Regular tester data is untouched.

At the original transaction-attempt review checkpoint, independent review inspected all seven screenshots and verified all seven image hashes plus 254 event hashes. It confirmed the same two persisted null `requirement_kind` values and a second handoff mapping defect: the saved JSON uses `businessNeed`, while the visible Business justification section reads `need` and therefore displays a blank value. Six positive commands returned 200; all 14 negative calls returned `P0001` with unchanged surrounding snapshots. The original attempt did not prove mobile draft viewing by screenshot, so its report stays incomplete. The later read-only supplement above fills that viewing gap without changing the original report. Smaller UI findings are raw `handed_off` status text, a planning assumption described as lead time, and Cancel still shown after a successful save. None of these checks constitutes full WMS or human-pilot certification.

**Correction direction, implementation pending:** A classification-only patch is insufficient. The existing owner-revision form does not cover classification, cost center, funding codes or solicitation requirements. Reuse the canonical Create Request form and governed creation contract for replenishment-bound handoff completion, with explicit `materials`/`services` choice and real required inputs. Preserve recommendation acceptance and separate route confirmation. Atomically create/link the request and mark the recommendation handed off only when validation succeeds. Do not invent pricing from stock cost, funding or evidence, and do not weaken role/training checks. Verify canonical draft semantics and the creation-authority dependency for every current replenishment-management persona before implementation. The two existing incomplete synthetic drafts remain retained; their repair is not authorized as an automatic replay.

The separate access-error candidate is independently approved in its narrowed form: SDK authentication events keep their existing authority-clearing behavior. Parent checks passed 73 Auth, 9 WarehouseApp live-wiring, 69 KB and 90 handbook tests. The direct production build completed with exit zero and a 17,016.10 KiB precache. The reviewer independently passed 88 Auth/WarehouseApp tests. These are local candidate results; deployment and actual UAT 503-to-Retry recovery are not yet confirmed. The eight-second bound rejects late state updates but does not cancel underlying network requests. Slow service responses may therefore show a recoverable fail-closed error.

- Build `4b2a8e1` remains the selected live target. A new run uses two exact synthetic zero-stock products owned by `1390f33a-bec0-4400-b5b2-717359f2442d`, separate Operations Associate and Procurement Lead accounts, desktop 1440 and mobile 390. No PO or physical stock movement is intended.
- Attempt `19-16-56-100Z-4a6df0fd` stopped at the first independent readback after the parent bridge missed its 120-second deadline. The old attempt was not resumed. Fresh database reads proved both products remained seeded-only before another attempt.
- Attempt `19-22-54-200Z-d2eff930` completed three fresh bridge readbacks, verified the ordinary browser identity, then stopped at a form-fill timeout before any browser business command. Independent post-failure reads again found no recommendation, request or activity, and all 17 downstream counters zero for each product. Own-user learning bootstrap and authentication did occur; this is not a zero-write claim for those services.
- Offline Chromium reproduced a test-selector defect: exact `getByLabel('Rationale')` misses a controlled textarea once its text is part of the wrapping label. The exact accessible textbox locator works. The runner correction adds a named failed step and safe scoped screenshot, retains all prior assertions, and passed 54 local tests. This corrects the test, not the business workflow; another live result is still required.
- The earlier focus diagnostic incorrectly marked draft loss for the same selector reason. Visual inspection of both saved screenshots shows the draft text remained. Corrected live checks in `access-baseline-4b2a8e1-1789327600646` confirm `normalFocusRetainedDraft=true` on desktop 1280 and mobile 390. The draft-loss claim is withdrawn.
- A separate controlled 503 capability-read failure still displays **No warehouse access** on both views instead of a retryable verification error. This is the confirmed app defect. The local fix distinguishes pending/error/confirmed denial, with bounded verification and explicit Retry. It does not change the existing SDK auth-event clearing behavior. Deployment and strict live retry checks remain outstanding.
- Earlier CI checkpoint (superseded by the Latest Live Checks above): `34775851767` passed its contract/persona/onboarding preparation gate. All six route jobs have now failed; the desktop governed-transaction job is running and mobile is queued. The desktop 1440 report flags an overlap on the wrapped original-order link in Returns. Its screenshot looks unobscured; DOM hit-testing must distinguish a genuine blocker from an inline-link bounding-box gap before any fix or test change. Other viewport failures must be inspected separately. This is not a passed CI run.

Evidence is retained under `outputs/wms-signoff/sep14-replenishment`. Prior attempts are failures, not certified checkpoints. No regular tester data was removed.

The database correction, Inventory entry and guidance are now released to UAT. The new form and contextual help were checked through an ordinary Operations Associate session, without saving a recommendation. This is not full transaction certification. SMTP is excluded. No isolated-role accounts or credentials were created in this review.

## What We Confirmed

Operations has an existing `warehouse.recommend_replenishment` capability, but its only recommendation panel was inside a page requiring `warehouse.view_procurement`. Adding a link to that page would still fail and would expose the wrong viewing boundary.

The live public recommendation RPC checks the correct effective capability, then calls an older implementation that requires one of three unrelated Procurement/viewing capabilities. At `2026-09-13T17:43:40.871Z`, an ordinary Operations Associate session submitted an intentionally invalid zero-quantity recommendation. Instead of reaching quantity validation, UAT returned HTTP 400 / `P0001`: **Procurement authorization is required**. The product reference was the deliberately nonexistent `WMS-REPLEN-GUARD-PROBE-NO-WRITE`. Independent database readback at `17:44:01.624606Z` confirmed zero matching recommendation rows. This is a reproduced authorization failure and no-write negative probe, not a successful replenishment journey.

The older implementation also updates active recommendations by product even after acceptance or handoff. That can replace the approved quantity/rationale while leaving the linked request unchanged. The correction must reject stale edits to decided snapshots atomically, not rely only on a disabled button.

## Fix And Verification Plan

- Keep the public RPC, existing effective capabilities, Procurement decision sequence and audit. Align its inner action checks with the outer checks; do not add broad Procurement grants.
- Permit the existing update/repeat behavior only while the recommendation is still `recommended`. Reject an overwrite once it is `accepted` or `handed_off`. Keep the active-product unique index and require failure to leave the recommendation, linked request and audit unchanged.
- Add a recommendation-only action in the authorized inventory product context. Show the selected product and authorized inventory data, not suppliers, spend, management buttons or links to restricted Procurement records. Quantity/rationale and any lead-time planning assumption must be explicit; do not label an assumed lead time or bounded history as a supplier forecast.
- Read the selected product's active recommendation state, not an arbitrary first 200 records. A failed read, pending permissions, revoked permission or unknown submit outcome must not become a fresh empty form or automatic retry.
- Test the complete ordinary-user recommendation, Procurement acceptance and linked draft handoff; repeat at desktop and mobile widths with persisted actor/quantity/audit checks. Keep malformed/forbidden calls and duplicate/decided-snapshot attempts separate from successful-flow evidence.

At `2026-09-13T17:54:29.24028Z`, live metadata confirmed the partial unique index on active product recommendations and the existing SELECT policy (`warehouse.view_inventory` or `procurement.view_dashboard`). The public RPC allows authenticated/service execution but not anonymous execution; the inner implementation denies authenticated and anonymous direct execution. Its old definition MD5 was `92c9cac13f202e62afcb81cda0928813`; the public wrapper MD5 was `bcb0f480d1e7206457f42ec8bb76e4f7`. The correction does not propose widening these read policies or execution privileges.

## Database Verification

The reviewed migration was applied as `20260913175711_align_replenishment_action_authority_and_snapshot`. Fresh readback at `2026-09-13T17:57:28.381651Z` confirmed the action-specific inner checks and the accepted/handed-off conflict predicate. The public wrapper MD5 stayed `bcb0f480d1e7206457f42ec8bb76e4f7`; the inner MD5 became `797cededb436b00531a3260cd87674cb`. Public and inner execution privileges remained exactly as recorded above. No role or SELECT policy was widened.

Parent and independent reviewer each ran the 12 isolated SQL tests successfully. The fixture loads the actual recommendation table, August 4 implementation and August 13 wrapper/rename/ACL, with limited supporting tables and simulated capability evaluators. It checks absence/revocation of authority, actual anonymous wrapper denial, direct-inner denial, unchanged outer body/ACL, supported transitions and refusal to overwrite decided snapshots. A single-backend PGlite test is not a true two-session concurrency test or full live Procurement integration.

At `2026-09-13T17:58:11.090Z`, the same ordinary Operations Associate zero-quantity probe on application `9c712ba` now returned **Quantity, stockout risk, and rationale are required**, showing that the obsolete authorization rejection no longer intercepts the request. Independent readback at `17:58:37.026671Z` confirmed zero probe rows. This does not establish successful recommendation creation, acceptance or handoff. Before/after security advisors reported the same 13 informational RLS-without-policy findings and no new finding category/count; these existing notices were not removed by weakening private-table policies.

## Audit Improvements

The signoff contract now explicitly requires recommendation, acceptance and handoff on both desktop and mobile. The registry has 48 checkpoints / 94 evidence records, adding six requirements without crediting older role-only results. All 107 contract/evidence-verifier tests passed after two new tests first failed against the old registry. These are synthetic verifier tests, not live business completion. Pilot scenario P16 is prepared and remains blocked until the release and live journey are checked.

The separate inbound KB candidate corrects receiving, inspection, putaway and continuing-hold owners, distinguishes reader access from execution, and explains existing actor separation. It preserves the flow edges, node/evidence IDs and previous screenshot bindings. Parent full-shell tests passed 769 with one skip; an independent reviewer checked 90 targeted cases. These checks do not recertify the old captures or the other unresolved flow-owner findings.

## CI Follow-Up

GitHub run `34736109548` was still queued on superseded build `75e733b`; its two transaction jobs had not started. Its six route jobs had completed earlier. Cancellation was requested after checking its exact identity and state, and it became terminal/cancelled at `2026-09-13T17:44:23Z`. Existing evidence was retained.

The waiting run `34772058143` then started and failed at lint, before new transactions. Its six lint errors were missing explicit Node `URL`, `process` and `console` imports in two warehouse layout-test files. The same errors were reproduced locally, then fixed with imports, not disabled rules. Warehouse lint passed. Tracked-file lint passed in the other completed packages with seven existing warnings; a command-length limit prevented that invocation on Warehouse, which was checked separately. Warehouse, Work and shell lint subsequently passed. An unfiltered whole-worktree lint run remains affected by an unrelated, untracked Finance browser test; that file is outside this release and was left untouched. Fresh CI after the final candidate is still required.

Run `34774032526` on `7f34bbe` passed lint but failed in `modules/learning/src/scopedReadinessCandidates.test.ts` at the existing-simulations fingerprint, before live role/transaction jobs. It expected `c36ea6b318331e30c3ce63fdd0a0e7e5d452dfb2778f52742521b7449927892b` and received `6302f690c98c4a4f8349db0d75398722d09e11091df9d55a0608a69da1355a9d`. The actual content delta is being investigated; no fingerprint has been replaced to suppress the failure. Missing artifact/bundle errors are downstream of that gate, not evidence that transactions ran.

Source investigation confirmed the cause: the `10cb745` RBAC alignment feeds dynamic capability-outcome generation in the Learning catalogue. All 27 simulation instructions/checkpoints/choices and versions remain unchanged, but six outcome arrays change (one only reorders). Requirements grow from 54 to 55: seven existing v1 requirements gain outcomes and a mandatory BI Analyst capability-practice requirement replaces generic guided practice in its existing v1 curriculum. Replacing only the RBAC input with its pre-`10cb745` baseline restores both original golden hashes. This is a source training-versioning defect, not stale CI. It does not prove changed live assignments or certifications: those are retrieved through governed database RPCs, and no publication or live assignment change was performed here. The separate correction will preserve reviewed v1 outcomes/membership explicitly and identify new pending coverage instead of automatically certifying additional capabilities. The existing golden fingerprints remain unchanged while that correction is prepared.

## Remaining Limits

The training candidate now pins the original source metadata and keeps both original golden fingerprints unchanged. It retains 54 default requirements, 33 role curricula and 27 existing simulations, with 12 role/capability pairs explicitly pending versioned review rather than auto-assigned. Parent verification passed 298 Learning tests, typechecking and lint, 794 shell tests (one skip), 57 documentation tests and the direct production build (17,014.69 KiB precache). Independent review passed 61 focused Learning tests and 68 KB tests; it approved source-versioning and matching copy only, not live coverage or publication. The staged release-documentation gate passed with two operational and ten documentation files. Generated HTML is current across 43 sources. The browser blocked the local-file URL, so no fresh visual handbook capture is claimed.

An additional seven-file Learning SQL/source-verifier run failed because its bounded authority model rejects later, unmodeled SQL migrations, including August 16/17 warehouse changes. That failure is retained for baseline investigation; no checksum, SQL allowlist or assertion was relaxed. The most recent completed CI run `34774705223` on `300437d` separately confirms the original simulation fingerprint failure (1 failed / 283 passed Learning tests), with route, transaction and cleanup jobs skipped. A successful local candidate suite is not a successful fresh CI run.

Independent baseline verification subsequently ran the exact seven-file command against an in-memory tracked `300437d` snapshot without changing the worktree or index: both baseline and candidate report 84 tests, 80 passed and the same four failures in `verify-learning-schema.test.mjs`. The tracked 236-migration model emits 913 diagnostics across 72 later migrations; the unrelated untracked optional-privacy migration adds nine diagnostics locally. Excluding that input only for comparison produces the same diagnostic hash as HEAD, `17c35cc7dcbb1fdb84c0a6efd6e4cb1425eef987c3b361b142d23fcc83d4e380`. No checks were weakened. This proves a pre-existing unsupported verification model, not a new candidate regression, database safety finding or schema certification. Repair still needs a separately reviewed exact model of the later SQL.

The correction is deployed as application `4b2a8e1fb510f04b3c4eed3c1ea6740926eb7b1e`, UAT deployment `dpl_3bBowvnw1NSpZwbtGYXKBBnKJdSW` (READY). Health at `2026-09-13T18:52:11.7099421Z` reported status `ok`, that exact commit, `appEnv=uat` and the intended Supabase project. Post-deployment readback at `18:52:13.583689Z` retained the same seven targeted published versions and metadata fingerprint `07908fd49b043ec12daa5028c1ec3e3d`. This narrow comparison does not certify all assignments, training coverage or database state.

A separate existing browser session displayed **No warehouse access** before reload. Live role rows still showed core Staff, Warehouse Operations and Warehouse Operator, effective with no expiry. Reload recovered the ordinary Operations Associate inventory page without signing in or changing roles. The initial client's exact build and request failure were not captured, so this is an observed recoverable access-state symptom under investigation, not an established training regression. Do not credit reload recovery as a fix or weaken authorization to hide it.

Source review found a plausible P2 path: a rejected capability refresh clears effective capabilities but retains verified user/roles, while Warehouse interprets an empty capability set as confirmed missing role. Same-user authentication events also clear capabilities before verification without resetting the initial-loading flag. Existing auth (23) and warehouse wiring (2) tests pass but do not cover these full recovery transitions. These files are unchanged between `300437d` and `4b2a8e1`; deterministic regression and a bounded error/retry state are being prepared separately. This is not yet the proved cause of that live session or a released fix.

Live ordinary-user KB navigation confirmed the new training clarification at the exact UAT release. A 1280x720 inline screenshot shows readable, non-overlapping text, but Safe recovery is still a dense semicolon-joined paragraph instead of separate steps. That existing presentation needs a focused readability correction; this capture is not archived/hashed screenshot certification. CI `34775851767` on `4b2a8e1` was in the unit/contract step at the last check, with no failed step yet; final CI results remain pending.

Read-only UAT review found seven published v1 versions among eight targeted requirement keys; BI capability-practice was absent. The operator and Procurement stored outcomes are narrower than the pinned source definitions. Consequently, source pinning must not be represented as database parity. The synthetic repository regression preserves narrower server outcomes and an empty server curriculum without manufacturing certifications. The targeted metadata fingerprint at `2026-09-13T18:35:45.624493Z` was `07908fd49b043ec12daa5028c1ec3e3d` (seven version rows); this is a narrow baseline, not a full-database snapshot.

The five newly classified capabilities were present in the mutation rules but had zero published curriculum outcomes and zero active certifications at readback. `learning.is_certification_required` is actor-scoped: it joins effective role curricula, published/effective versions and matching outcomes. A null-auth administrative call alone is not a user-level authorization test. Repeating the read with explicit authenticated claims for the existing checkpoint Operations Associate returned the expected actor UUID; replenishment recommendation and export preparation were effective, certification-required false, with neither an active certificate nor an emergency exception. Export review and the two Procurement actions were not effective for that account. This documents the existing publication-dependent gate, not a service-role bypass, newly completed training or certification for all roles. No database function, publication, assignment or credential was changed by these reads. Missing training coverage remains open for a separate versioned release.

Application `7f34bbe15e74ab19a2af653c297f01f910c066d7` was deployed as `dpl_Eudvnyj7ag8dSHbuS421eB8bdhvU` (READY). Health at `2026-09-13T18:16:03.787Z` reported that exact commit, `deployment.appEnv=uat`, `deployment.supabaseProjectRef=kkoitlvydytdhlpxhuah`, reachable Supabase and static assets. The direct final build and 793 full-shell tests passed (one browser-specific skip). The release-documentation gate passed against `9c712ba`. Independent final guidance review passed 15 focused tests with no blockers.

On the deployed Inventory item PERF-SEP12-0101, the ordinary CI checkpoint-v1 Operations Associate opened the new form. Available/minimum were both zero, the planning assumption was 14 days, and status was verified as no active recommendation. Save was disabled with zero quantity/blank rationale, became enabled with positive quantity and a reason, and Cancel closed the form. Reopening cleared the unsaved input. No Save action was clicked and the sample record was not changed. Contextual help loaded the matching released instructions from the product Help link.

Independent database readback at `2026-09-13T18:21:01.878109Z` confirmed zero recommendation rows for the exact sample product `perf-sep12-product-101` after the browser checks. This is a no-write form check, not a seeded handoff journey.

Inline screenshots were visually reviewed at 1440x900, 390x844 and 320x740 in the existing dark theme. The desktop form was centered with readable field labels and distinct header/footer. On narrow screens the form reflowed and its body scrolled, but the pre-existing PWA update banner overlapped Rationale and, at 320px, part of Cancel. This is an actual remaining visual defect, not a clean mobile certificate. The isolated fix is to keep optional install/update notices below the shared modal overlay while retaining manual Reload/install behavior. These inline captures are not archived, hash-bound evidence records and receive no signoff-registry credit.

That overlap is now fixed in application `300437dfa7d37d0cf9aa5ef2be609d5daacc9469`, deployment `dpl_Eqr781RG8ZSxSbFUgfPuGGSPErWL` (READY). Exact-build health at `2026-09-13T18:29:01.821Z` confirmed UAT, the intended Supabase project and reachable assets. After reload, the actual waiting-update notice was still present in the DOM while the recommendation dialog was open. Fresh screenshots at 390x844 and 320x740 show unobstructed Rationale and footer controls. A settled 1440x900 capture also shows the notice behind the modal overlay. One immediate viewport-transition screenshot was cropped and discarded rather than treated as evidence. Keyboard Tab from Rationale reached Cancel inside the dialog; Cancel closed it and the pending Reload button became visibly enabled again. No Reload or Save command was clicked, and no device-install prompt was triggered. The optional install notice has local layer/accessibility coverage only, not a live installation test.

The two-class fix passed 42 parent focused Warehouse tests, 67 KB tests, 49 documentation tests, Warehouse/shell lint and the direct production build (17,010.94 KiB precache). Independent review passed five PWA tests and found no scoped blocker. KB troubleshooting, four maintained handbook/spec sources, the consolidated HTML and tester log were updated. This verifies the demonstrated modal overlap without certifying all forms, light theme, actual mobile hardware or the transaction matrix.

Post-follow-up independent readback at `2026-09-13T18:31:06.07873Z` again found zero recommendation rows for `perf-sep12-product-101`. The form was closed and the temporary viewport override reset. The test account remains on that inventory item for continued review; regular tester records were not changed.

The reviewed Inventory component adds no new Procurement route or grant. It checks only the selected product's active status before opening the form and again before saving. Independent review caught a misleading disabled form in accepted/handed-off states: its numbers came from a proposed draft, not the approved record. The corrected component hides those inputs and Save for these states. All 81 focused UI tests passed independently; the parent full Warehouse suite passed 1,085 tests. The parent combined SQL/signoff-contract/evidence run passed 119 tests, and 49 handbook/release-verifier tests passed. These are local tests, not live transaction or screenshot certification.

The production build compiled, typechecked and generated its pages successfully, but its Turbo wrapper did not exit after reporting completion. That wrapper was stopped by verified process identity. A direct `pnpm --filter @intra/shell build` then completed with exit zero, including typechecking and static generation; the service-worker precache was 17,010.95 KiB. No application or test behavior was changed to hide the wrapper issue. The unrelated untracked Finance lint issue remains outside the scoped release.

No new recommendation journey, true two-session race, screenshot certificate or production signoff is claimed yet. The isolated-role provisioning adapter remains an offline candidate; planned database work means this review cannot attest that DDL is paused. The rest of the WMS transaction/exception/recovery matrix, cleanup and actual user/device pilot remain open.
