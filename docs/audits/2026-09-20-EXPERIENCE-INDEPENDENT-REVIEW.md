# September 20 Experience Independent Review

Status: reviewed corrections and the five-migration UAT rollback preflight passed. After parent installation, read-only attestation matched all 36 directly declared function bodies and the 36 restrictive read guards; **not activation or complete live-journey sign-off**. See Rollout Preflight for evidence and remaining gates.

Checkout: `C:/Users/NormanArisDeocareza/Projects/mwell-intra-onboarding`, branch `codex/uat-launch-blockers`, HEAD/base `2c2adfb6b4004a505369f3286dea044d32199517`, with concurrent uncommitted agent changes. Scope: UX-07/08/15 and independent security/logic review of the new Events custody and stock-origin conversion migrations, retained writers, and reachable user flows. This records all findings from this independent review, not a replacement for the complete Sep20 audit catalog.

## Findings and Retests

| Finding | Observed failure or hazard | Correction and independent retest |
| --- | --- | --- |
| UX-07, P2: Warehouse wayfinding | Warehouse Home replaced suite navigation; mixed-role users lacked explicit workspace exits. | All workspaces and My Work available in header, mobile More, and Account; module home explicitly named Warehouse home. Desktop/mobile navigation contracts pass without role changes. |
| UX-08, P2: Alerts versus Inbox | Hundreds of shortages appeared as undifferentiated actions; suite bell still lacked a visible Inbox label. | Warehouse Alerts grouped/searched/filtered with reconciled counts, actionable versus informational shortages, and next-owner handoffs. Viewing/filtering does not resolve issues. Suite trigger and sheet consistently say Inbox; opening does not mark read. Rendered 44px/mobile-label and existing sheet/focus contracts pass. |
| UX-15, P1: destination-denied links | Warehouse-only admin saw Insights/Procurement links; Pricing saw Warehouse stock-approval links it could not open. | Links use destination current effective capabilities, not source-module visibility or underlying roles. Scoped seller Events read capability is accepted without broad view_events. Negative and mixed-role navigation tests pass. Fulfillment return-link/status fixes belong to its owner, not this reviewer. |
| IR-01, P1: conversion versus retained receipt serial trigger | Real retained normalize_inventory_unit_serial rejected approved conversion of a clean posted receipt serial because the product changed. Initial conversion fixture omitted that trigger. | James added narrowly bound current-transaction/actor/immutable-line conversion authority while preserving original posted receipt identity. Actual retained trigger now included; approved conversion and subsequent status transitions pass; arbitrary SKU/serial edits and pending/held/non-clean claims deny. |
| IR-02, P1: valid conversion actors could not reach UI | manage_returns operator was blocked by the kit tab; Product-only approver lacked a Product surface; inspect_quality-only actor failed Warehouse route admission. | James added Product-only recipe workspace/UI without operational custody data. Fulfillment owner added separate conversion tab and effective route admission. Product-only approval, operator and inspector entry, and unauthorized direct selectors pass. No broad grants added. |
| IR-03, P1: serial outcome DTO mismatch | Event allocation JSON omitted serialized; UI would submit bulk quantity without required serial identities. | Ptolemy added serialized to the server projection. SQL assertion and scoped Events component/outcome tests pass. |
| IR-04, P1: competing legacy custody sources | Retained reserve/issue could create untracked allocations after opt-in. Before opt-in, product-only locks could race event enable; acknowledgment could then encounter unresolved custody. | Ptolemy guards allocation source and locks the event before legacy product/allocation locks, including pre-gate events; fulfillment locks event before order. Executable gated reserve denial and installed function lock-order assertions pass. True multi-connection race proof remains pending. |
| IR-05, P2: rescheduled seller window | Stored assignment dates could authorize posting before a rescheduled event or imply an unintended extension. | Authorization now intersects the stored assignment window with current Manila event dates. Early access and silent extension deny in SQL tests. Extension requires explicit owner reassignment; post-event correction policy is not supplied by this fix. |
| IR-06, P1: cancellation stranded released stock | Independently reproduced cancellation after release but before acknowledgment: two issued units, zero custody sources, then retained acknowledgment rejected the closed event. | Outstanding handover now prevents closure/cancellation. Independent probe includes retained close guard, verifies rejected cancellation leaves event active, then runs actual acknowledgment and captures exactly two units. Pass. |
| IR-07, P1: returned allocation unreserve corrupted totals | Actual retained cancel_allocation accepted returned -> cancelled, reducing legacy lifetime issued total to zero while immutable custody still held two. | Captured allocation status is restricted to issued/returned. Actual record_return_v2 then cancel_allocation denial preserves both totals at two; matching physical-return settlement also passes. |
| IR-08, P1: settlement could contradict or grow custody | Caller-authored returned/lost/damaged/rekit counters could settle outstanding stock. Empty settlement could precede uncaptured handover; later demand/acknowledgment could grow a locked ledger. | Settlement compares canonical physical returns, requires zero remaining custody, rejects unsupported exception counters and outstanding handover/demand. New demand/order/source growth is blocked after submitted/approved settlement. Owner tests and independent empty-ledger/order probes pass. The defensive acknowledgment test deliberately constructs inconsistent fixture state; no deployed trigger was disabled. |
| IR-09, P2: demand input validation | During retest, fractional quantity denied with integer-parser text instead of expected wording. Ptolemy then reproduced acceptance of duplicate product lines. | Ptolemy added bounded, positive-whole-quantity and unique-product validation after the replay boundary. Final multi-product persistence, replay, fractional/duplicate denial and unchanged approval-authority tests pass. |

## Exact Verification

Independent local runs on September 20 used Node 24 at `C:/Users/NormanArisDeocareza/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`. Vitest commands were `node_modules/vitest/vitest.mjs run <files below> --reporter=dot`, executed from the stated module directory. These are separate focused runs, not a full-app suite result.

| Working directory | Test files / check | Result |
| --- | --- | --- |
| Repository root | Node `--test scripts/verify-stock-conversion.pglite.test.mjs modules/events/tests/custody.pglite.test.mjs` plus external `event-cancellation-review.mjs` | **57/57 pass**: conversion 32, Events 21, independent probes 4. |
| `modules/warehouse` | `src/app/modules.test.ts`, `src/app/notifications.test.ts`, `src/components/AppShell.test.tsx`, `src/components/ReplenishmentControlPanel.authority.test.tsx`, `src/pages/ProcurementPage.test.tsx`, `src/pages/ProcurementPage.navigation.test.tsx`, `src/pages/PurchaseOrdersPage.test.tsx`, `src/WarehouseApp.live.test.tsx` | **147/147 pass**, 8 files. |
| `modules/warehouse` | `src/pages/FulfillmentPage.conversion-route.test.tsx`, `src/pages/FulfillmentPage.navigation.test.tsx`, `src/components/StockConversionPanel.test.tsx` | **21/21 pass**, 3 files. Read-only retest of other owners' changes. |
| `modules/finance` | `src/FinanceApp.test.tsx` | **13/13 pass**. |
| `modules/product` | `src/StockConversionRecipes.test.tsx` | **7/7 pass**, including actual ProductApp with no Warehouse grants. |
| `modules/events` | `src/EventCustodyWorkspace.test.tsx`, `src/EventsApp.test.tsx`, `src/seller.test.ts` | **16/16 pass**. |
| `apps/shell` | `lib/accountMenuBounds.test.ts`, `components/NotificationBell.test.tsx`, `components/NotificationBell.interaction.test.tsx`, `components/NotificationBell.polling.test.tsx`, `components/AppShell.notificationScope.test.tsx` | **21/21 pass**. Legacy literal-title failure was observed before replacing it with rendered 44px/visible-label assertions; sheet scrolling, focus and dismissal requirements retained. |
| `apps/shell` | TypeScript `--noEmit --incremental false` | **Blocked** by generated `.next/dev/types/routes.d.ts(118,1): TS1128`. No generated output edited; parent owns clean restart/retest. Prior scoped Warehouse/Finance typechecks passed, not a substitute for final shell/full-app proof. |
| Existing preview `http://127.0.0.1:3217/work` | Playwright memory-session browser at 1440x900, 390x900, 320x900 | Inbox measured **44x44**, visible text, no horizontal document overflow at all three sizes; 320px capture visually inspected. Memory Inbox is disabled, so live unread/polling behavior was only mocked in component tests. Preview left running. |

Earlier failures are retained as evidence, not omitted: missing Inbox label/copy regressions failed before implementation; the old account bounds literal failed before semantic test repair; conversion's retained serial trigger conflict and Events cancellation/unreserve defects were reproduced before owners' fixes. Intermediate runs had Events 20/21 due fractional-error wording and conversion 31/32 due the new repeat-cycle fixture reusing the unique receipt inspection tuple. Both final suites pass. The latter was a fixture correction using a distinct accepted base-return inspection, not relaxation of the production uniqueness constraint.

External independent probes/captures are under `C:/Users/NormanArisDeocareza/.codex/visualizations/2026/09/18/01a0b4fc-57b0-7351-84ee-b8baeb3a422c`: `event-cancellation-review.mjs`, historical `migration-review-probes.mjs`, and `suite-inbox-1440.png`, `suite-inbox-390.png`, `suite-inbox-320.png`. The historical conversion probe predates the expanded owner fixture and is not the current rerun entry point.

Reviewed migration SHA-256 values after the final local SQL run:

- `supabase/migrations/20260920025525_gated_event_seller_custody.sql`: `76A0DD7EB3FCA99FA97C0833F171DC0D6EF9CC78B3D4A0BAE13086E844C6BC9D`
- `supabase/migrations/20260920025920_approved_stock_origin_conversion.sql`: `09B0F32ABEB94FB4F53CF3C72913A31A468D9AEC2F26597741799D3065A33283`

## Open Proof Gates

- **Real PostgreSQL contention:** PGlite queued submissions and source lock-order assertions do not prove independent-connection reserve/enable, acknowledgment/settlement, return/outcome, or competing conversion lock behavior. Exercise these in an approved isolated PostgreSQL environment before release.
- **Complete migrated/live authority:** fixtures retain relevant existing functions/triggers but simplify schemas and stub auth/current capabilities and learning readiness. They do not prove the full migration chain, deployed role grants/revocation, PostgREST exposure, published approved seller learning, storage object ownership or evidence authority. Parent's batch-evidence review and Leibniz's persistence/custody work require their own final evidence.
- **Complete effective custody journey:** conversion repeat-cycle tests synthesize return/inspection rows and some inventory status changes. They do not execute the entire receiving -> Quality -> conversion -> demand -> release -> independent acknowledgment -> seller outcome -> physical return -> Quality -> recovery chain through all effective public RPCs. Original receipt identity and local rollback are proven only to the tested fixture boundary.
- **Remaining policy:** prospective Events settlement deliberately fails closed for loss, damage, rekit and post-window corrections without an approved supported disposition/correction flow. No seller date extension, broad role grant, historical backfill or settlement bypass was introduced. Required business exceptions need explicit owner policy and separate proof.
- **Integrated local and native:** final parent full suites/typechecks/build and clean Next preview retest remain separate gates. Component mocks and desktop Playwright emulation do not establish real-device touch, native mobile/PWA installation, keyboard/assistive-technology or unstable-network behavior. No live notification delivery/readback or SMTP test occurred.

## Ownership

### Parent Integration Follow-Up

After the independent snapshot above, the parent regenerated Next route types through `next typegen`. All ten affected package typechecks and the final production build passed. Package regression runs finished with 3,729 passes and one existing opt-in browser test skipped; the final isolated SQL run passed 102 checks. The temporary memory preview at port 3217 was stopped. The final production preview at port 3031 produced 44 desktop/mobile captures with zero recorded page errors, overflow or automated axe findings. These resolve the generated-type and integrated-local gates only. Native concurrency, complete effective live custody, deployed authority and real-device/pilot gates remain open as described above.

The initial review phase did not edit SQL, Fulfillment, Product, Events, other agents' implementation, unrelated QA/docs, or generated Next output. Its source edits were confined to `apps/shell/components/NotificationBell.tsx`, `NotificationBell.test.tsx`, `NotificationBell.interaction.test.tsx`, and `apps/shell/lib/accountMenuBounds.test.ts`; local Next instructions/docs were read first. The later explicitly authorized SQL ownership is recorded below. This report is reviewer-owned and intentionally **not added to the report/content catalog**. Parent may classify it for manual inclusion. This reviewer performed no commit, push, deployment, persistent live mutation, SMTP change or broad role grant.

## Rollout Preflight

Reviewed against actual UAT `kkoitlvydytdhlpxhuah`, PostgreSQL 17.6. Parent reports protected hosted runtime candidate `998906fee743dfc9baaa895c5d713d152a488de2`; this review does not independently attest that deployment. The four base SQL sources remained byte-identical. Read-only catalog review verified retained-function patch anchors, source triggers, stock uniqueness constraints, private function ACLs, RLS, learning table columns and effective orientation. Both the original four-source rehearsal and final five-source rehearsal installed under one transaction and rolled back successfully. No migration-history repair was attempted.

| New Finding | Fix / Verification | Remaining Limit |
| --- | --- | --- |
| IR-10, P1: raw Warehouse exposure | UAT exposed Warehouse via PostgREST with authenticated SELECT and eleven unconditional primitive policies; kit metadata also admitted generic core directory readers. A scoped seller UI/RPC did not prevent direct raw reads. New `20260920080131_gate_warehouse_raw_reads_by_current_capability.sql` intersects all 36 authenticated-readable Warehouse tables with restrictive, current effective capability checks; replaces kit's generic directory admission with Warehouse/Product capability admission. Existing row predicates remain intersected. No role or table grants added. | Parent must apply this exact reviewed guard and verify installed state before seller activation. Local success alone was not treated as deployment proof. |
| IR-11, pre-publication Learning phase dependency | Actual UAT `learning.is_certification_required` joins an effective published role curriculum. An unmapped seller therefore has `core.has_live_cap('events','record_event_outcome')=true` before publication; the separate readiness helper remains false and prevents prospective event enable. The initial rehearsal's expectation of mutation=false was incorrect and was corrected, not hidden. | After mapping, prove mutation=false before actual learner completion and true only after authorized learning evidence. No mapping/certification/attempt writes were made by this reviewer. Content approval is not activation approval. |
| Marketing membership boundary | Authorized rollback-only fixture used existing synthetic Marketing employee `ed569969-b1dc-4fa6-9135-3f98e68d533c`, retaining its actual member scope `7e55e54e-86cd-4157-9fdb-7616be83e340`. Original roles were temporarily expired and a seller role inserted, then expired for revocation proof. No role DELETE was used because its trigger revokes certifications. | Core directory reference tables have existing authenticated read policies; Procurement retains existing owner/collaborator paths. Those are not department-membership capability grants. No extra roles, fabricated membership or departmentless Learning exception was introduced. |

### Exact Sources

| Migration | SHA-256 |
| --- | --- |
| `20260920025455_atomic_quality_batch.sql` | `788be97c10a6a5873cd6bf6ece625e0fa1078ea04e73f31061712986a3fbb955` |
| `20260920025525_gated_event_seller_custody.sql` | `76a0dd7eb3fca99fa97c0833f171dc0d6ef9cc78b3d4a0bae13086e844c6bc9d` |
| `20260920025920_approved_stock_origin_conversion.sql` | `09b0f32abeb94fb4f53cf3c72913a31a468d9aec2f26597741799d3065a33283` |
| `20260920030900_event_seller_learning_readiness.sql` | `686b0e0b30450559828def055bfd27c207ec64668172643e5b258bee9d569dee` |
| `20260920080131_gate_warehouse_raw_reads_by_current_capability.sql` | `2322b5163d912f701578fd1804c4480f63de2708998e3333574b987f73f33fdf` |

Final rehearsal artifact: external `sep20-five-migrations-rollback.sql`, SHA-256 `3b5962e2de6c0693bf8600023a4b4af596a2b2e7d0457a59d06012fc904925db`, generated by `build-sep20-five-rollback.mjs` in the external evidence directory above. Executed using linked CLI `db query --file ... --output json`, READ COMMITTED, local lock timeout 2s / statement timeout 30s, final explicit ROLLBACK; exit 0. Source hashes and linked project were checked before generation. Failed intermediate rehearsals aborted without persistence: the phase expectation above, then a fixture expiry-before-effective constraint. The final fixture respected the existing temporal constraint; no production source was changed to accommodate either failure.

Nine actual UAT personas (Warehouse operator/supervisor, Procurement, Finance, Marketing, business unit, Insights, Product and Legal) had the expected `core.has_live_cap` checks true. Legitimate Warehouse hydration and Events/Product input counts were retained; kit metadata did not regress. Existing Legal does not require raw Warehouse custody. Seller-only effective roles with actual Marketing membership returned zero rows across all 36 raw tables and the invoker custody view, while custody read capability was true and Warehouse inventory, broad Events, Procurement dashboard and core directory capabilities were false. Scoped unassigned listing returned empty. Expiring seller authority retained zero raw reads despite forged stale role metadata. Certification and membership fingerprints also remained unchanged *inside* the role-only fixture.

Before/after rollback fingerprints matched exactly: catalog `d303827157c04abe65b88a207e544ccc`; roles `efcb29050111afe1ef3beb26f1644feb`; capabilities `c981b245d5b990588c0eb1dd465a8b51`; role-capabilities `c8b438729cc03d55c0b557ec346b7e79`; user roles `1774d74013fdfe4df5374cc200361979`; memberships `4dc77602ddab9bf927f8fc35c9f08216`; certifications `68f8183e42526b655ebe622251b735cc`; learning rules `cd9a70e58391dfe4988f6d4e330651c5`. Unchanged counts: profiles 141, roles assigned 390, memberships 104, certifications 979, requirements 51, curricula 41, activity 168042, inventory units 691, stock levels 27, movements 78, allocations 8, returns 3, commands 1064, migrations 242. Catalog digest includes function definitions/ACLs, relation RLS/ACLs, non-internal triggers and policy definitions/mode/roles.

### Retests and Ownership

- `node --test scripts/verify-warehouse-raw-read-policy.pglite.test.mjs`: **12/12 pass**. Initial empty-migration run was **6 fail / 3 pass** and reproduced the leak. Final tests cover actual role registries, legitimate mixed roles, seller/staff/vendor/no-role denials, owner-policy intersection, expired/future/inactive/revoked grants, stale JWT metadata, custom action certification expiry, helper ACLs, restrictive SELECT-only policies and Marketing member scope. Fixture retains actual core resolver SQL but uses minimal tables and controlled certification responses.
- Ptolemy's independently executed native PostgreSQL 17.10 RLS sidecar: **17/17 pass**; report/source inspected and exact guard hash matched. It additionally invokes actual seller listing/ledger/outcome/retry RPCs as authenticated with real current-role resolver bodies and proves other-event/raw-read/revocation denial. Native data/certifications remain synthetic. Native contention and batch sidecar evidence belongs to Ptolemy; parent received those separate reports.
- Independent Learning review: training/scoped-readiness **14/14**, render-only publication helper **4/4**. Actual six-checkpoint text, server rules, manifest and helper read; manifest/content/rule digests independently recomputed; UAT pinned orientation and active audit custodians verified read-only. Approval artifact `docs/audits/2026-09-20-SELLER-LEARNING-INDEPENDENT-REVIEW.json`, SHA-256 `83f688571dac0953071011e04e4e0d2b391f0b76423c5584ca315025f792e041`, is hash-bound AI review only, `humanReviewClaimed=false`, `activationApproved=false`. Existing test-profile UUIDs are audit FK attribution, not human-review claims. Parent and Noether received the path/hash.
- New owned repository files in this preflight: the fifth migration, `scripts/warehouse-raw-read-policy.fixture.mjs`, `scripts/verify-warehouse-raw-read-policy.pglite.test.mjs`, and the JSON review artifact; this Markdown report updated. Original four migrations and other agents' files unchanged. No TypeScript runtime changes in this phase. No staging, commit, promotion, SMTP or persistent SQL execution by this reviewer.

### Post-Install Attestation

Parent subsequently applied the exact five-source bundle as `sep20_experience_custody_and_current_read_isolation`. Independent read-only UAT query confirmed that history entry and **243** migrations. All **36/36 directly declared function bodies** matched SHA-256 values computed from the reviewed local source after CRLF-to-LF and outer-whitespace normalization; zero missing/mismatched definitions. This body comparison does not separately re-hash the six dynamically patched retained function definitions. The read guard's complete `pg_get_functiondef` SHA-256 is `aaab157eb900a98dc10bf218a73ff0e7d3c8aa5d247daa3fd56372540f5d3793`, exactly matching Ptolemy's native-tested installed definition. Catalog also confirmed **36 restrictive authenticated SELECT guards**, anonymous helper execute denied, authenticated execute retained, and helper SECURITY INVOKER. External reproducible read-only query: `sep20-definition-attestation.sql`, generated by `sep20-definition-attestation.mjs` in the evidence directory above. No installed schema/data writes were performed by this attestation.

**Final verdict:** no remaining five-migration installation blocker found; reviewed raw-read guard is now attested installed. Seller activation still requires approved stage-by-stage publication/mapping, the real Marketing-member seller's effective Learning journey and certification boundary, and actual scoped event operation. Rollback/catalog checks, native synthetic concurrency and local browser emulation do not establish a complete live custody journey, real-device behavior, delivery/SMTP or production rollout success. Baseline broad TRUNCATE/TRIGGER privileges were observed but no reachable direct SQL exploit was demonstrated; this SELECT guard neither adds nor remediates those legacy privileges. Runtime/migration source frozen at this handoff; parent owns further release actions.
