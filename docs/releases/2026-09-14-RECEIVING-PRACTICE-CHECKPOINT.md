# September 14 - Receiving Practice And Sourcing Recovery

## Release Status

Initial receiving recovery released as `b3b74983c7260d5b0ff3a2049b94c67d10e3d0be`; the layout and sourcing read contract followed in `c1b09d63e358db8a495b993537f9817621ee8c3b`. Build `53e25997e2ea17cdcd78ddb89efb9ef188ea743d` was verified at **2026-09-14T04:38:11.324Z**, deployment `dpl_CJMMKEGvV2fQyMjwmm72EDgLbYSZ`. Current UAT is **`ec646b515d3da3519e4f0787ef911830f85f69fc`**, deployment **`dpl_3SEseDRVfNiRvoSqhesxD3kUWaxH`**, with canonical health verified at **2026-09-14T05:38:42.028Z** for UAT project `kkoitlvydytdhlpxhuah`. Finish review, Pricing, pick-location and blind-count changes are deployed; deployment alone does not verify their live journeys. SMTP is excluded. This note does not certify the complete WMS or a human pilot.

## Changes

The released layout collapses the optional task chooser, removes its nested scrolling, keeps action buttons compact with full accessible names, and places the next required action before the optional return to work. The verified operator curriculum version 2 also receives its correct display role. Desktop 1440x900 and mobile 390x844 live administrator screenshots were reviewed after deployment; mobile Start appears above the bottom navigation and neither viewport has horizontal overflow.

The existing-event sourcing correction extends the existing sourcing read with event-bound evaluation evidence, while retaining narrower variance-history visibility and server-derived review eligibility. The app no longer needs the variance-review-only endpoint just to open sourcing. The database migration was applied to UAT after a baseline/ACL review; the independent variance function and both function ACLs are unchanged. Security advisors show the same 13 informational no-policy notices before and after. Local validation passed 414 Procurement tests and 25 policy/SQL tests. On live build `c1b09d6`, the normal Procurement Lead created and reopened draft event `28bf735b-b3fc-4226-8078-1b1bfbcaba55` on the original request. The existing-event read passed on desktop and mobile. No invitation, award or duplicate request was created.

That retest found a deadline display defect: a saved 5:00 PM Singapore deadline appeared as 9:00 AM in the editor. The database still held the correct 09:00 UTC instant; no second Save was performed with the incorrect editor value. The correction, released in `53e2599`, converts the persisted instant into device-local date/time components. Independent review passed 56 focused tests in each of Asia/Singapore and UTC. Ordinary Procurement Lead unchanged Save passed on that build on desktop 1440 and mobile 390: the same event remains the only retained draft, with zero invitations and responses, database deadline **2026-09-24 09:00 UTC**, and editor **17:00 Singapore**. Live evidence covers Singapore only; other timezone cases remain unit-test evidence. The retained readbacks and screenshots are indexed in `outputs/wms-signoff/sep14-isolated-onboarding-observation/sourcing-deadline-live-verification.json`.

- Receiving practice records its saved-draft checkpoint at receipt review. Learners no longer need to pause just to satisfy this checkpoint. Final submission, required evidence and controlled quality routing remain mandatory.
- Emergency-access wording now says approval is required. It does not imply that access has already been granted.
- Procurement verifies sourcing state before requesting event-specific reads. A confirmed request without a sourcing event shows the valid not-started state; a failed read offers Retry sourcing.
- KB, the standalone manual, training guidance and technical specification explain these changes.

**Deployed follow-up: Finish review**

After receiving practice has a confirmed terminal completion, with no checkpoint error or operation in flight, **Finish review** replaces the coach's Back, Resume later and Exit training actions. It closes training and returns through the existing router to `/onboarding`, removing the training query. In-progress Pause/Exit and other practice coaches are unchanged. Failed or pending completion cannot take this terminal route. This does not submit another receipt, replay a checkpoint, refresh authority or grant access; **Refresh status** remains the checklist's separate refresh action.

The six-file candidate passed 1,107 Warehouse tests, 312 Learning tests, module typechecks and focused lint. Independent review passed 312 Learning plus 25 Receiving tests with no blockers. These are offline results; live Finish review navigation remains pending.

The deployed Pricing correction keeps Warehouse price context read-only. **Open governed pricing** appears only for a signed-in, settled profile with effective Product `view_readiness` and `view_pricing`; other users see Product-owner guidance. **Load more products** exposes additional rows in batches of 12. Bundles with missing constituents show **Unavailable**, never partial or zero totals; complete zero-cost bundles still show zero. Cost multipliers are labeled **markup**, with unchanged calculations and actual-margin display. Seventeen focused tests pass, including wrapping-class checks rather than fresh device geometry evidence. No grants or business commands changed. On live `ec646b5`, the ordinary isolated Pricing account's desktop-1440 check reached **12, then 24, then 368 of 368 products**, with 369 table rows including the header and no Load more control at the end. The unauthorized Product link was absent and Product-owner guidance was visible; actual margin remained **37%**, and all **three incomplete bundles** showed **Unavailable**. The `pricing-handoff-fixed` and `ecallproducts` screenshots are retained in the isolated-onboarding evidence folder. Those screenshots also exposed long-name table overflow, now assigned to a separate UI follow-up. This is a bounded functional desktop pass, not a layout or mobile pass.

The deployed pick-location card correction preserves an existing order-level location. When that source is unset, it resolves confirmed picked lines through their saved bin IDs, deduplicates repeated IDs and displays each bin's code plus optional label. Distinct bins with the same label remain distinct; missing references stay visibly unavailable. An unpicked order says **Pending pick**; a later state with no saved pick location says **Pick location not recorded**. It does not assign bins, allocate stock or change any command or permission. Independent review closed the duplicate-label finding after a failing regression and correction. The corrected pair passed 49 tests locally and a separate parent rerun passed all 49; the reviewer's earlier independent run passed 48 tests on the prior pair. Live card verification remains pending.

The deployed blind-count correction hides pre-submission variance amounts, balanced status, expected shortages and variance-only filtering on desktop and mobile markup. Entries and required evidence remain visible; unexpected serials still block submission. A failed submission keeps the sheet blind, while a successful submission retains the existing result and approval flow. No payload, calculation or authority change is included. On `53e2599`, the isolated Logistics check only established that missing evidence disabled submission; its pre-submit balance feedback prompted this correction. No count was submitted and no image was uploaded; that draft was cleared afterward. On `ec646b5`, a matching entry of **5** hid expected quantity, variance and balanced feedback, while **Submit count** stayed disabled without evidence. No count photo or submission was made. This verifies a bounded pre-submit UI guard, not count completion. The viewport-switch tool was a no-op during the new checks, so there is **no fresh `ec646b5` mobile verification claim**.

## Evidence

For the `ec646b5` release, the parent passed **50 clean tracked-source documentation tests**, generated-HTML `--check` and the release documentation gate. The full Warehouse run started before the final card freeze and reported **1,134 passed and one failure**: the new distinct-bin test encountered cached old source while the pair was changing. It is not recorded as a full-suite pass. The subsequent frozen Fulfillment rerun passed **49/49**. Earlier test totals below remain historical results, not substitutes for an uninterrupted full-suite run on the final source.

Reviewed UAT migration **`20260914053733_uat_vendor_fixture_atomic_cleanup.sql`** was applied separately. It provides the service-role-only CI vendor cleanup contract; its entry points revoke public/anonymous/authenticated execution and grant execution to `service_role`. It adds no ordinary-user grants or SMTP/business workflow changes. Application does not certify a vendor journey or mean CI cleanup has executed. CI 192 remains the latest CI result.

The next certification source release records the exact temporary approval-group membership before test setup and passes that evidence to independent cleanup. Cleanup uses a compare-and-swap update, preserves unrelated concurrent members, and verifies absence afterward. Missing evidence or an uncertain update blocks cleanup credit instead of reporting success. Independent review passed 255 offline checks with one existing live-contract skip; the parent reran 70 focused regressions successfully. These checks do not replace a completed live CI run. SMTP behavior is unchanged.

The live isolated Warehouse Operator completed five of nine assigned requirements on build `f8d437d`. Receiving practice then stopped because attempt `9eddf857-a376-4e03-b034-10c4ee3ac326` contained a validated `complete` checkpoint but lacked the published `draft-saved` checkpoint. Its status remained `in_progress`; it was not manually certified or resubmitted.

Local checks: 1,103 Warehouse tests, 299 Learning tests and 377 Procurement tests passed. Three new receiving regression cases failed before the adapter fix and passed afterward. The emergency-access copy regression also failed before its fix. Warehouse, Learning and Procurement typechecks passed; 56 handbook/release-documentation tests passed. These are local results, not live completion evidence.

After deployment, the same learner used Back to return to evidence review and confirmed the condition through the normal UI. The server recorded the missing `draft-saved` checkpoint with outcome `ready_for_review` and completed the original attempt at 02:29:52 UTC, because its earlier validated submission was already recorded. No direct certification write or second receipt submission was used. The refreshed live checklist showed **6 of 9 complete** and an active Receive Stock certification. This is recovery evidence, not a fresh uninterrupted attempt. Three required exercises were still open at that checkpoint; their later completion is recorded below.

Desktop 1440x900 and mobile 390x844 screenshots of the same resumed learner were visually reviewed, not separate completion runs. The updated emergency-access wording is visible. Those earlier captures identified an expanded task picker and long role list pushing mandatory progress below the first viewport, nested scrolling, and long Start labels squeezing task titles. The released layout correction described above addresses these findings. These captures do not certify every role, screen or control.

## Still Open

The same operator subsequently completed Quality, putaway and pick/pack guided practices through the live UI on build `3b2d766`. Read-only database evidence confirms three passed attempts and their validated checkpoints; the UI shows **9 of 9 complete** and five active capability certifications. This completes that assigned learning path, not the operator's business transactions or a fresh uninterrupted receiving attempt.

The preserved replenishment request `req_6297bc50-5f26-4a75-a455-fd3bb64cbff7` was reopened as its normal Procurement Lead on live UAT. The confirmed route, original two attachments and valid **Not started** sourcing form displayed without a variance-review error. The subsequent draft-event creation and reopen are recorded above. Later evaluation, invitation and award stages are not certified by this bounded sourcing retest.

All seven isolated personas in run `357c12f2-153a-474e-a48a-896e2c36e35c` have now completed their assigned learning: Operator **9/9**, Supervisor **4/4**, Logistics **4/4**, Operations **3/3**, Pricing **4/4**, Administrator **4/4** and Multi **11/11**. Operator through Pricing evidence belongs to `3b2d766`; Administrator and Multi final UI evidence belongs to `c1b09d6`, not the later `53e2599`. Shared credit is not a separate executed practice. Pricing has zero earned certifications; its checklist completion does not prove a gate unlocked. The administrator has 19 active certifications, and its exact published curriculum alias is now included in live `53e2599`; unknown versions remain unknown.

Multi's fresh receiving attempt `b15eb15a-f16a-47de-a013-7b2cc48207ac` completed normally on `c1b09d6`, without Pause, Back or reload. Persisted validated `draft-saved` and `complete` checkpoints culminated in a passed/valid attempt at **04:24:35.159044 UTC**. Final desktop/mobile checklist evidence and `outputs/wms-signoff/sep14-isolated-onboarding-observation/multi-final-readback-20260914T044344Z.json` confirm 11 distinct requirements, eight actual passed attempts and 20 active certification rows covering 15 unique capabilities. Bounded actor/time/marker checks found no real receipt or stock movement from this training exercise; this is not a universal residue or Storage-absence assertion.

The later ordinary-user Operations handoff completed on `53e2599` at **05:09:14.650412 UTC**: Operations requested two synthetic units, Supervisor approved, Multi allocated/picked/packed, a distinct Supervisor released, and Operations acknowledged through mobile 390. Request/order `41c92680-badc-45d0-9e00-9afbd80a5b9d` is closed/completed; stock reconciles **7 to 5** with one quantity-two `fulfillment_release` from the owned inbound destination bin, reservation released and packaging `[]`. The combined-role packer's release control was withheld by the UI; no backend denial probe was executed. `outputs/wms-signoff/sep14-isolated-onboarding-observation/operations-multirole-handoff-live.json` retains the readbacks and exact lineage. The completed records and one synthetic acknowledgement Storage image remain retained. This is a nonphysical, bounded journey pass, not full certification or a cleanup/zero-residue claim. Its stale pick-location label prompted the separately deployed correction above; this earlier journey is not relabeled as an `ec646b5` run.

CI 191 passed all six route/viewport jobs and both independent cleanup jobs. Desktop passed 44/45 workflow records, mobile 45/45; both lacked vendor application-to-Legal handoff evidence. CI 192 stopped because the generated standalone handbook did not match the changed release source. Its route and transaction stages were skipped, not passed. The parent regenerated the handbook from tracked release sources, normalized tracked text to LF in the clean release worktree and passed 98 documentation tests. That repair does not turn CI 192 green; the vendor fixture still awaits preflight and full certification remains open.

Still open: live Finish review navigation, completion of Pricing and pick-location retests, blind-count submission/review scenarios, and fresh mobile verification on `ec646b5`; remaining business-role journeys and later-stage sourcing validation; full desktop/mobile CI certification and real-user/physical-device testing. Existing test records are retained for reconciliation. SMTP remains untouched.
