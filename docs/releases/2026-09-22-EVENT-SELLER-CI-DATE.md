# Event Seller CI Date Correction

## September 22 UAT Deployment Receipt

Application `9989a0d20fd6126f4ce61ebe5c9e38b9bda0feb1` was deployed to the UAT project and promoted to the canonical UAT domain. Deployment ID: `dpl_7oWQMZJ6f7gfmAFVCLxUh6GheiKw`. Health at 2026-09-22T14:08:10.464Z confirmed that exact commit, UAT environment and project `kkoitlvydytdhlpxhuah`.

Only migrations `20260922111953` and `20260922123940` were installed after a scoped dry run. The public governed approval wrapper stayed unchanged; the private delegate remains inaccessible to API roles. No active DOA policy, role, learning completion or historical approval was rewritten.

The existing synthetic PHP340 Operations request passed final approval through the live UI as its named Operations Lead. Unassigned Procurement Lead eligibility remained denied. Database readback confirmed Approved status and identical hashes for the two prior decisions. Desktop/390/320 confirmation screenshots had no page-wide overflow. These synthetic test marks do not represent real-person approval or a supplier commitment.

Updated September 23 (Asia/Singapore): canonical UAT now serves `9823059bbe08fd1949105a5901683188e5716115`, deployment `dpl_FTMPTes5Uj8v3sEcDc3uyz3vaPSy`. Runtime sources are unchanged from `9989a0d`; the successor corrects named-approver test fixtures and contracts. [CI208](https://github.com/normanwashere/mwell-intra/actions/runs/35748805822) completed successfully at 2026-09-22T17:24:14Z: all 12 jobs, six route widths, 48/48 maintained workflows per desktop/mobile transaction run, both independent cleanups and the complete evidence bundle. Canonical health at 17:31:24.643Z confirms the exact commit and UAT backend. Bundle SHA-256: `25afc13332595d1a9b1688afca80fb9966a1f1a40fe73bf2e619121df4bde8ba`. Updated team handoffs document source-copy/Bitbucket transfer and remaining recipient setup, human pilot, seller-to-Finance and production gates. SMTP is untouched. Candidate evidence below is historical, not a current-status assertion.

## Why Run 203 Stopped

The named-seller ledger test created its event using PostgreSQL `current_date`, which follows the database session timezone. Seller access correctly evaluates event dates in Asia/Manila. Run 203 reached the check at 17:10 UTC on September 20, already September 21 in Manila. Its synthetic September 20 event was therefore expired, and the real predicate correctly rejected it with `Not authorized: event custody read`.

The same access failure was reproduced locally by running the unchanged fixture under Pacific/Honolulu while Manila was on the next date. The normal UTC run passed earlier in the UTC day, explaining the time-dependent result.

## What Changed

Only the isolated SQL test fixture and regression coverage changed. Fixture start/end dates now explicitly use the same Manila business date as the existing seller predicate. Regressions cover UTC, a negative-offset timezone and a positive-offset timezone; exact opening/closing midnight boundaries; assignment revocation, expiry and future validity; inactive profiles; expired roles; own-entry filtering; and cross-event denial.

No application code, live database function, migration, role assignment, event date, stock, onboarding completion or SMTP setting changes. Actual event expiry remains enforced. Historical September 20/21 test events are not extended or backdated by this correction. The deterministic boundary test substitutes a clock only inside its disposable PGlite database; production SQL is unchanged.

## Verification and Release Limit

The failing reproduction was observed before changing the fixture date expression. On September 22, the focused suite passed 15/15 tests. The complete 11-file event-custody/access CI command then passed 151/151 tests, with zero failures, cancellations or skips, using Node 24 and isolated test processes. This larger result includes the focused suite; the counts must not be added together. JavaScript syntax and whitespace checks also passed.

A passing local gate is not full hosted certification. A new exact-commit hosted run must still complete preparation, six route viewports, governed transactions, independent cleanup and the evidence bundle before full certification can be claimed.

The handbook was regenerated with this release note registered. Its 90 catalog, guide and generator regressions pass with zero failures or skips using the repository's original LF source bytes, matching the hosted checkout. The documentation-currentness check also passes. No screenshot was recaptured or newly certified by these source tests.

This test-only correction does not change operating instructions, so no new user training or KB behavior is introduced. The Data, Technical and Tester handoff packs remain drafts while the user-prioritized CI repair is completed.

Later September 22 local candidate, not part of deployed `46b0b85`: the fresh live Operations request exposed a hardcoded final-tier administrator restriction and a misleading training error. The earlier candidate treated that role-name restriction as correct; the approved direction now makes final authority follow scoped effective DOA, not `procurement.admin`. Migration `20260922123940_procurement_final_approval_doa_authority.sql` follows the tier-guard candidate: its private actor predicate permits any current Procurement `approve_request` role on an active employee, while its private final-DOA predicate requires exactly one current matrix and one matching active final assignment, the exact pending-step matrix version, the request's department/category and inclusive amount band, and the named pending assignee. Live training, no-self-approval and independent activation checks remain; administrator status is not an override. The governed decision rechecks after request/step and policy locking. Private helpers remain inaccessible to API roles. No history, roles, active policy, training or SMTP is changed. The earlier Operations-only Procurement Lead draft remains inactive and is not recommended or activated. The event's Marketing department must not be confused with the request's Operations department. No completed seller or Finance chain is claimed.

The live inbox candidate removes the redundant local-tier veto only in Supabase mode; `resolveTiers` and memory-mode authority remain unchanged. It now binds eligibility to the exact server-returned pending `stepId`, request department/category/amount and revision, step request/matrix versions and assignment, reviewed facts, and relevant actor/role/capability readiness. Missing or mismatched step IDs deny. Context changes and refresh loading/errors immediately invalidate permission, close the sheet and clear the note/signature; late responses cannot restore stale authority. The click path checks the binding, and rejected decisions recheck after refresh even with unchanged rows. Failed refreshes offer retry without replaying a decision. Explicit profile ID/email/name/kind fields avoid closing the sheet on irrelevant title/last-active refreshes.

The initial role-veto baseline failed three of fifteen checks. The subsequent stale-context baseline failed 33 of 48 checks, and the profile-metadata regression separately failed before narrowing the identity key. Final local verification passes 51/51 inbox regressions, 538/538 Procurement tests, typecheck and focused lint; the focused checks are included in the suite, not additive. Confirmed local verification is 110/110 checks (63 expanded PGlite, 18 prior tier-guard and 29 CI-contract checks), plus an independently reviewed and main-rerun 16/16 native PostgreSQL 17.10 result (13 scenarios, two safety checks and one parent test), with no skips. The native server is shut down; hosted CI service execution and exact-build live validation remain pending. The verified migration SHA-256 is `0e4463cd122704b23a8cb1db3f23dde945a946cf16c9d08fa6c006e2d2111e77`. Final decisions require READ COMMITTED and reject non-finite assignment-band bounds; a blank maximum remains unbounded. If the policy-table lock reports an update in progress, nothing is saved and the action asks for a retry shortly. The required isolated native gate is wired into hosted CI but has not run there. The offline handbook is regenerated from these maintained sources; its source/generator checks remain separate from live application certification. Hosted CI, installation, deployment and exact-build live retesting remain pending; live `46b0b85` does not include this candidate.

The local signature correction is now tested: saved governed snake_case signatures and existing camelCase signatures map to the same display model; the UI submits the existing governed RPC keys. No signer, timestamp or historical image is manufactured, and external/non-PNG previews are rejected. All 487 Procurement tests pass, including 42 signature-boundary cases, three actual request-page markup cases and four browser layouts, plus typecheck. These counts overlap and must not be added together. Actual component renders at 320/360/390/1440 verify wrapped approval emails and attachment metadata, separated 44px Download actions and a loaded synthetic PNG. Offline system fonts and mocked reads are used, not live accounts. Deployment and exact-build live verification remain pending.

CI205 later exposed two mobile route defects in deployed 46b0b85: the request detail overflowed at 320px, and a header-only Department hierarchy target was 42px high at 390px. The candidate fixes wrapping and gives the Department target a 44px minimum. Ten Department browser regressions pass across five widths and two data states; the ninth required suite is retained by the updated 29 passing resilience contracts. The recorded expanded helper-group run passed all 107 tests. CI205 preparation and four route widths passed, but these two route gates failed. Its desktop vendor submission also timed out waiting for a response, so Legal handoff is unproven; investigation is separate from SMTP, which remains excluded. No full certification is claimed.

Follow-up: hosted run 204 passed the seller-access gate, build, CodeQL and documentation checks, then failed the separate CI-resilience test. Its browser-suite assertion had not been updated when the event-demand selector regression was added to the workflow. The corrected assertion reads structured YAML, retains the exact eight reviewed browser/helper suites and execution-order requirements, and rejects missing, duplicate, conditional or failure-ignoring checks. LF and CRLF source are both covered. All 27 resilience tests and all 97 tests in the eight-file browser-helper group pass locally. This changes only the test contract and documentation; the workflow does not drop or skip a check. A fresh hosted run is still required, and the live vendor and event transaction journeys remain separate verification obligations. SMTP is unchanged.

The CI205 desktop vendor artifact confirmed an application defect, not SMTP: the draft had every required fact but no declaration timestamp. The real signature pad had captured a signature, yet readiness validated the unsigned draft and kept submission disabled; the submit handler was the only place that added the timestamp. The local candidate validates readiness and submission against the same captured-signature payload. It does not persist a signing date before submission, reuse an old signature, waive required facts or alter server authority. Three of seven new regressions failed against the old implementation; all seven and the full 244 Legal tests now pass, along with typecheck/lint. The archived live application remains version 1/draft with no submitted snapshot. Scoped cleanup is reported in that artifact, not a full independent-cleanup certificate. Live submission, replay and independent Legal handoff remain unproved until the candidate is deployed and retested.
