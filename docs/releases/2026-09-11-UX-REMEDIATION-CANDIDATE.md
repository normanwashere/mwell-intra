# September 11 UX Remediation Candidate

Status: local candidate only. No live publication or deployment is recorded by this note. This is separate from the earlier September 11 UAT performance and wording releases.

## Documentation Scope

The source audit is `outputs/sep11-role-ux-audit/findings.json`. Instructions are aligned to the current working-tree implementation, not to the audit's proposed controls. The parent owns app testing, acceptance evidence and final status. Source review and a generated handbook do not establish that all 26 findings are resolved or that all screens are certified.

## App-Wide Layout Verification

- The actual working screens now share a compact header and clearer section boundaries. Home, Admin, Warehouse, Procurement, Finance, Events and Insights also received page-specific layout changes. Existing actions, permissions and workflow order are retained.
- Screenshot review found the shell stylesheet scan omitted Events, Product, Insights and Work as direct sources. The scan now includes every module; a regression test protects that coverage. The production CSS contains the Events desktop three-column row rule. The PO heading now identifies Purchase orders rather than the signed-in user's first name, and Finance scope badges share one wrapping row.
- This pass ran 1,637 unit/component/contract checks successfully, with one pre-existing shell browser-oriented test skipped: UI 60, Work 13, Procurement 296, Finance 93, Legal 213, Events 61, Product 60, Insights 38, Shell 727 plus the new stylesheet contract 1, and focused Warehouse 75. Procurement/Finance were rerun after the final heading/badge edits; repeats are not added. This is not full transaction certification. The optimized production build, including TypeScript checking, passed.
- Before the final stylesheet correction, 92 primary-screen geometry checks covered 46 role/route pairs across 11 personas at 1440px and 390px. Screenshots and sampled visual review found test-harness-blocked read requests and the missing Events layout. Earlier geometry passes therefore do not mean clean-screen acceptance. Those read-only RPCs were checked in source and allowed in the harness; the harness now fails a screen with blocked requests and checks the actual Events desktop grid.
- The environment blocked starting the production preview on port 3022, so the post-build 92-screen retake is still outstanding. The existing port 3021 development preview was not stopped. The gallery retains pre-final screenshots, explicitly labeled as such. No live deployment or database migration was performed.
- A separate final-code visual check used the existing port 3021 memory/demo app as Marketing & Events Lead. Events showed aligned three-zone desktop rows and stacked 390px mobile rows; the mobile document was 375px wide inside a 390px viewport, with no horizontal overflow. Both screenshots were reviewed in the browser. This is a targeted demo check, not UAT-connected transaction evidence, and is not added to the 92 earlier captures.

## Implemented Instructions

- App-wide operational layout pass: `ModuleHero` and `PageHeader` now share one compact in-flow header implementation, with container-based action reflow. Section headings use full-width boundaries. Home/Admin launchers, Warehouse floor shortcuts/counters/report sections, Procurement filters/tables/forms, Finance sections, Event rows and Insights rows receive corresponding layout changes. This changes the actual application, not only KB screens. Commands, field names, query targets and business gates are retained. Primary-screen browser evidence is saved separately in `outputs/sep11-app-layout`; it is not certification of every transaction state.

- Preserved links: supported local query context survives sign-in only for authorized destinations. Section fragments alone are not guaranteed across authentication. My Work help targets its exact guide.
- Filters and records: Warehouse order status/search/channel and read-only order selection are addressable; department requests retain status/selection. Order counters clear search/channel. Insights areas, Admin directory state and Legal Cases/Lifecycle views have corresponding addresses. No unsaved action draft is represented by these links.
- All-task chooser: onboarding Change task includes searchable All eligible tasks. Compatible same-workspace return context remains; unrelated requirements are cleared. Task selection does not grant permission or learning credit.
- Notifications: the main bell covers only the latest 10 accessible notices. Supported authorized records have Open record. Warehouse alerts count active issues, not unread notices, and remain until the source issue is resolved. No complete notification history route is introduced.
- Audit history: each authorized search step checks up to 2,000 retained events and returns up to 50 matches, with UTC+08 date boundaries. Search unfinished can appear with zero matches; Continue searching checks older history. Older results opens known further matches and Previous revisits a page. Export current page CSV exports only displayed matches. Unapplied filters block export/paging. Retry audit history recovers reads; an unfinished search is not evidence that a record is absent.
- Vendor POs: the vendor header offers Applications and Purchase orders; the PO list provides Back to vendor portal and Retry purchase orders. Existing vendor scope and acknowledgement controls remain.
- Finance handoff: payment queue links open the PO Payment handoff section and offer Back to Finance. PO section navigation does not change receipt, Quality or payment authority.
- Read-only Finance receipt evidence: Open source record from a selected receipt close draft opens a scoped receipt summary and registered evidence labels. Linked close entries can offer protected evidence previews. Back to close draft binds the source again; full unsaved-field restoration is not claimed. Retry receipt recovers unavailable reads. Receipt-line/Quality detail beyond the summary remains a Warehouse handoff.
- Policy evidence: named requirements, evidence formats and business fields replace raw JSON. Supporting request attachment selects an existing file only; no new upload control is implied. Submission remains subject to governed review.
- Product handoff: Kit approval: Not required before handoff is separate from the WMS Kit publication status/version/reference. Active kit publication follows the approved Operations handoff, not the other way around. Acknowledgement does not publish a kit. A missing visible definition or unavailable publication read is not a prerequisite; explicitly required or unknown kit conditions still need Product resolution.
- Failure recovery: explicit read errors and local retry actions are documented only where present. Finance retries remain source-specific. Insights follow-up errors appear inside the dialog; an uncertain write must be checked before repetition.
- Handoff clarity: where Workflow status appears, read Current status, Next responsibility, Next step and any Needs attention message before using existing controls. Next responsibility is a role-level handoff description, not a person assignment, owner change, access grant or action authorization. Unknown or unavailable readiness is not ready; no warning is not proof that an action is allowed. Request approval, PO issue, receiving, payment, fulfillment release, recipient acceptance, return resolution, customer closure, Legal confirmation, Event settlement, Product acknowledgement, kit publication and Finance reconciliation retain their separate evidence and authority checks.
- My Work orientation: the My Work heading and primary assignments queue precede follow-ups. Open source remains the route to current record state. No work in this view describes only successfully loaded matching assignments, not completion of work in other departments. Queue unavailable offers Retry and must not be read as caught up, even if a count or earlier rows remain visible.
- Landed Procurement details: compact request/PO headers retain identity, status and amount before the workflow summary. Closed POs now direct Procurement/Finance to verify linked payment, remaining balance and settlement; PO closure and accepted evidence do not authorize payment release. Closure review unavailable offers Retry closure review; an unavailable goods-acceptance record offers Retry goods acceptance. Failed supporting reads do not establish that no responsibility remains.
- Stock-request rejection and receipt: the requester asks the approver for the rejection reason and creates a new corrected request if needed. The original remains rejected and immutable; there is no current stock-request rejection-reason input, in-place resubmission or system-linked revision. This is distinct from Procurement request revision. Awaiting receipt confirmation names Recipient / another authorized staff member. This does not grant authority, and the person who released the items cannot confirm receipt; reference and proof remain required.

The handoff-clarity pass is an additional local source/documentation update. The earlier parent-reported acceptance below is preserved as historical candidate evidence, not final test evidence for these new summaries. The parent will supply final application test statistics and desktop/mobile acceptance for this pass separately. No workflow, owner assignment, role grant, Knowledge Base requirement publication, SQL application or deployment is authorized by this documentation update.

## Parent-Reported Acceptance

### Final Handoff-Clarity Verification

The parent completed the following after the workers' handoff. These supersede the in-progress detail status below, without rewriting the earlier audit or initial failed runs:

- 1,619 unit/component tests passed: UI 58, My Work 13, Procurement 292, Finance 91, Legal 213, Events 61, Product 60, shell 727, and a targeted Warehouse set of 104. One shell browser-oriented test was skipped. Legal/Events were rerun after compact-header changes; repeat runs are not added. This is not the full Warehouse suite or full live transaction coverage.
- Strict shell TypeScript checking and the optimized Next.js production build passed on Node 24. A build is not a deployment.
- 44 unique role/screen/viewport checks passed: 22 common-workspace checks covering all 11 personas and 22 detail checks across eight personas, at 1440px and 390px. Six Warehouse link interactions passed, including order-line scrolling, linked fulfillment navigation and browser Back restoring request context. Final targeted header and wording recaptures replace earlier captures in the unique count.
- Browser evidence used the local candidate on port 3022 connected to UAT read data, with operational writes blocked. No live stock, payment or approval transaction was performed. Initial missing read-RPC allowlist entries and incorrect table selectors were corrected in the harness; initial evidence is retained. PO auxiliary-read failure recovery gained explicit regression tests.
- KB/manual content checks remain 88 knowledge and 87 documentation tests, separately scoped and not added to application totals. No evidence-certification flags were changed.
- [Screenshots and plain-language update](../../outputs/sep11-handoff-ux/index.html) includes test logs, coverage boundaries and the unrun actual-user pilot. Finance close and return summaries have component coverage in this pass, not live detail screenshot certification. The prior readiness migration remains unapplied and this candidate remains undeployed.

For the handoff-clarity pass, the parent reports the following confirmed scope. This documentation worker did not rerun these application, SQL or browser checks:

- QA: 67/67 passed, comprising 57 isolated SQL checks and 10 static checks. This is not full browser end-to-end testing. The stock scenario covers 10 transitions across 4 actors: rejection, a new corrected request, approval, allocation/picking/packing, independent release and requester acknowledgement, plus 6 denials. Recovery does not revise or reopen the rejected stock request and does not establish system-linked revision lineage.
- Common-workspace browser checks: 22/22 passed across all 11 roles. Final detail-summary checks were still running at the parent's handoff; no final detail count or outcome is recorded here. Common-workspace checks do not certify every detail state or transaction.
- Actual human pilot: not run. `outputs/sep11-handoff-ux/REAL_USER_PILOT.md` is a proposed observation script with blank records, not evidence of participants, timings, usability outcomes or completed user acceptance. The parent will add the final evidence note separately.

The earlier parent-supplied local-candidate results below, including the seven-module Node 24 rerun and independent PGlite verification, retain their original scope. They are not added to the new handoff totals. This documentation worker recorded them without rerunning the application or database tests. The [user-facing remediation report](../../outputs/sep11-ux-remediation/index.html) retains the consolidated results and follow-up context.

- Shell tests: 724 passed, 1 skipped, 0 failed. Strict shell typecheck and optimized Next production build passed. A successful production build is not a deployment.
- Separate seven-module Node 24 rerun: Legal 180, Finance 70, Product 35, Insights 37, Events 46, Learning 274 and Procurement 235, totaling 877 passed; exit code 0. This suite total is reported separately from the shell results. No deduplicated or aggregate unique-case count across suites is claimed.
- Separate independent SQL regression: the parent ran `node --test modules/procurement/commitment-readiness-boundary.pglite.test.mjs` on Node 24 with exit code 0, 9 passed and 0 failed. The isolated PGlite tests reproduce the old wrapper's ownerless-request failure and verify the candidate wrapper's read controls, denials, private policy predicate, requirements response and access-control permissions. These 9 results are not added to an aggregate unique-case count across suites.
- Cold links: six Knowledge Base, onboarding and Legal link checks passed across both desktop and mobile widths.
- Initial scoped browser captures: 11 roles, 38 captures, 34 passed and 4 failed. These initial failures remain part of the record; subsequent retests are reported separately, not used to rewrite the initial totals.
- Follow-up checks: both PO retests passed. A fresh 390px Finance session passed login, receipt and payment checks; the earlier timeout was not reproduced in that fresh run. This does not establish the cause of the earlier timeout or blanket closure of all initial failures.
- Targeted layout checks: Admin Manage passed at 1440px and 1280px. At 390px, the first Events action was observed at vertical position 473px; this is a targeted observation, not certification of every Events screen or state.
- The parent's live SQL check confirms that all eight historical requests exist with `requester_id` null. The live `procurement.commitment_readiness` wrapper incorrectly treats a null owner as a missing request; this is a database read-boundary defect, not eight missing records. Candidate migration `20260911060022_restore_ownerless_commitment_readiness.sql` restores the `FOUND` existence check and qualified read access while retaining the requirements response and private readiness predicate. It does not invent or assign owners; ownerless requests keep `canRecordAcceptance` false without an explicit active reviewer assignment. The isolated regression above passed, but the database correction remains pending deployment and has not been applied to UAT. The eight live errors are therefore not reported as repaired.

These results support the tested local candidate only. They do not close all 26 audit findings, certify all screens, replace exact-control screenshot acceptance, or authorize live publication.

## Remaining Acceptance Limits

- UX-25 read-only Finance receipt evidence is source-aligned and the parent reports the fresh mobile receipt check above. The summary is not a complete receipt-line or Quality inspection viewer; that pass alone does not certify every evidence permission, draft-restoration or write-denial case.
- Role denial cases, older-event fixtures, uncertain retries, browser history and desktop/mobile states beyond the reported checks retain their own evidence requirements. The parent owns final finding-level disposition and verification/deployment of the historical ownerless-request database correction. Existing request ownership is not fabricated to suppress the errors.
- This note is not a finding-by-finding closure register. Long-page layouts, focus behavior, Events capabilities, Product prerequisites and other concurrent changes retain their own implementation and acceptance evidence.
- The candidate note has explicit local/not-live catalog, source-heading and legacy-route registrations. Neither handbook registration nor generation is deployment or acceptance evidence.
- Handoff source review confirms summary render sites in Procurement request/PO details, Warehouse order/department-request details and customer-return resolution/closure forms, Legal case detail, Event detail, Product readiness and Finance close-entry disclosures/edit/flag forms. This is not a claim that every queue, new-entry form or error state displays a summary, nor a rendered-screen or workflow test result.
- The earlier source review found that `purchaseOrderWorkflowSummary` treated every Closed PO as No further PO action. Final landed-source inspection confirms that Closed now requires payment/settlement verification, including correction or Finance follow-up when recorded evidence or balance requires it. The manual reflects that correction. This source check does not establish browser or financial-transaction acceptance; final detail evidence remains with the parent.

## Documentation Checks

Handoff-clarity follow-up, verified by this documentation worker on Node `v24.19.0` using `C:/Users/NormanArisDeocareza/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`:

The final landed-code wording update reran the same focused suites: 88 knowledge tests and 87 documentation tests passed, with 0 failures in each suite. These repeat runs are not additional unique test counts. Final detail-browser evidence is intentionally left for the parent's subsequent note.

- Focused knowledge rerun: 88 passed, 0 failed across `coverage.test.ts`, `content.test.ts` and `validate.test.ts`.
- Documentation rerun: 87 passed, 0 failed across `build-app-documentation.test.mjs`, `handbook-catalog.test.mjs` and `handbook-guides.test.mjs`. No new catalog entry, control or evidence-acceptance record was added for this pass.
- Ran the `docs:build` and `verify:app-documentation-html` script targets directly with Node 24: `scripts/docs/build-app-documentation.mjs` generated the handbook from 40 source documents, and the same script with `--check` passed. A separate generated-HTML DOM inspection confirmed the shared labels, role/access limits, My Work empty/error distinction, Finance disclosure instructions, prior test limits and PO caveat. The first optional DOM-check attempt could not resolve jsdom from the shell package path; rerunning against the installed pnpm dependency passed without changing dependencies.
- Scoped `git diff --check` passed. These are content, generator and DOM-content checks only, not browser layout, keyboard, transaction, application build, database or live acceptance tests. Final application statistics for the new handoff pass remain with the parent. No Knowledge Base requirements were published, no SQL was applied and nothing was deployed.

Earlier documentation checks are retained below with their original runtime and evidence limits; their counts are not added to the follow-up totals:

- Focused knowledge tests: 88 passed again after the audit continuation and Product/WMS wording changes, across `coverage.test.ts`, `content.test.ts` and `validate.test.ts`. Documentation tests: all 87 passed across the generator, catalog and guide-model suites, including the 8 catalog tests. These checks validate content contracts and provenance rules, not rendered app behavior.
- Coverage snapshot generator/check: 291 controls across 68 features, 53 existing evidence records, zero exact control evidence matches, 287 missing action-evidence bindings and all 291 rows still unverified. No evidence acceptance state was changed.
- `git diff --check` passed for the edited documentation and knowledge files.
- `pnpm docs:build` generated `docs/manual/index.html` from 40 source documents; `pnpm verify:app-documentation-html` passed. Initial missing catalog/heading registrations and the subsequent legacy-route test failure were corrected. The installed Node 20/pnpm 9 reported the repository's Node >=22/pnpm 10 engine warning; successful local documentation checks do not establish supported-runtime app certification. No application release or live publish was attempted.

## User Reference

See the September 11 local-candidate section in `docs/manual/MWELL_INTRA_USER_MANUAL.md` and the corresponding in-app feature guides. Previously deployed notes remain dated and are not evidence that this candidate is live.
