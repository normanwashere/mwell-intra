// Shared theme constants (framework-agnostic; safe to import from both the
// server <ThemeScript> bootstrap and the client theme hook).

export type Theme = 'light' | 'dark';

export const THEME_STORAGE_KEY = 'intra-theme';
